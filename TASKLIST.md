# Tasklist

This file tracks the work currently in progress. Only items listed here should be modified/implemented.

## Active

- [x] **Bugfix / UI — inventory bag action hover tooltip wiped by strip overlay:** Hovering **INVENTORY (from bag)** rows drew the item tooltip in the right panel, then RenderActionInfoStrip cleared the center overlay (early-out only covered gear **POOL**), leaving an empty center and orphan fragments (e.g. ×0.75)). Skip strip tooltip clear for HoveredInventoryPoolIndex, clamp bag-item tips to the center panel, and sync InventoryRightPanelLayout row Y with the renderer. Tests: InventoryRightPanelLayoutTests; docs: OVERVIEW.md.

- [x] **Bug fix / UI — exit prompt blank before divider:** `ColoredTextCoordinator.WriteColoredSegments` was dropping empty segment lists, so the exit menu’s leading blank never rendered after consumables. Empty lists now write blank lines. Menu keeps **one** blank before the top divider and **none** before the bottom; search no longer adds a trailing blank that stacked with the menu spacer. Tests: `ColoredTextCoordinatorTests`, `DungeonExitChoiceHandlerTests`. Docs: `OVERVIEW.md`.

- [x] **Data / Sheets — push log Open:gid + API cell counts:** Optional-tab push now reports Sheets API `UpdatedRows`/`UpdatedCells` (like ACTIONS) and an **Open:** `edit?gid=` link so a successful write is not confused with viewing a different empty tab. Tests: `SheetsPushPreflightTests`. Docs: `GOOGLE_SHEETS_INTEGRATION.md`.

- [x] **Data / Sheets — item triggers catalog tab:** Tab **triggers** (gid `42970568`) ↔ `Triggers.json` (`id`/`name`/`when`/`count`/`scope`/`mechanics`/`value`/`filters`/`channel`); Weapons/Armor **`triggerName`**; resolve at `ItemGenerator` via `TriggersLoader`; stamp CLI writes names not nested blobs; pull/push + Balance Tuning gid/checkbox. Tests: `JsonArraySheetConverterTriggersTests`, `SheetsPushConfigTests`, `EquippedItemTriggerTests`. Docs: `GOOGLE_SHEETS_INTEGRATION.md`, `OVERVIEW.md`.

- [x] **Items / trigger system expansion (66 identities + equip channel):** Swing-subject filters on item procs; `ONEVEN`/`ONODD`, `IFSLOT`, `IFUNARMED`, `IFCLASSTAG`; `hero_action_damage` same-swing; `Item.EquipEffects` / `ItemEquipEffectApplicator` (armor synergy, class primary, grant action/tag overlay); catalog demos for the 11 affordance examples; stamp `% 66`. Tests: `EquippedItemTriggerTests`. Docs: OVERVIEW / ARCHITECTURE.

- [x] **Items / base catalog trigger bundles (55 identities):** Stamp `triggerBundles` on every Weapons/Armor row via `ItemTriggerIdentityCatalog`; copy in `ItemGenerator`; `EquippedItemTriggerApplicator` + room-clear pass; bundle `value` magnitude fallback; dice/threshold on TURN. Item hover **Triggers** section via `ItemTooltipFormatter` / `ItemTriggerBundleDisplay`. Re-stamp `--stamp-item-triggers`. Tests: `EquippedItemTriggerTests`, `ItemTooltipFormatterTests`. Docs: `OVERVIEW.md`, `ARCHITECTURE.md`. *(Superseded/expanded by trigger system expansion above.)*

- [x] **Data / Actions — RESERVE POOL column + tag:** Sheet column **RESERVE POOL** (ensured on ACTIONS push) + registry tag `reserve_pool`; excluded from default weighted `Actor.SelectAction` rolls; still usable on combo strip. Settings checkbox. Tests: `ReservePoolActionTests`. Docs: `TAG_REGISTRY.md`, `GOOGLE_SHEETS_INTEGRATION.md`, `OVERVIEW.md`.

- [x] **Data / Sheets ? ACTIONS push deletes legacy columns K?Y:** On OAuth ACTIONS push, delete old `TURN CADENCE`/`ACTION CADENCE`/? triples plus compact `DURATION`/`CADENCE`/`MECHANICS` (`ActionCadenceSheetColumns.CollectLegacyColumnIndicesToRemove` + Sheets `DeleteDimension`). Keeps the authoritative `CADENCES` band. Pull still accepts compact fallback. Tests: `ActionCadenceSheetColumnsTests`. Docs: `GOOGLE_SHEETS_INTEGRATION.md`, `OVERVIEW.md`.

- [x] **Data / Sheets ? flavor tab PUSH:** Flatten `FlavorText.json` to long table (`section` / `bank` / `key` / `text`) via `FlavorTextSheetConverter`; OAuth push to tab **flavor** (gid `825117964`) when `pushFlavorTab` is on. PULL skips FlavorText. Balance Tuning checkbox + gid field; `SheetsConfig.flavorSheetUrl` / `SheetsPushConfig.flavorSheetTabName`. Tests: `FlavorTextSheetConverterTests`, `SheetsPushConfigTests`. Docs: `GOOGLE_SHEETS_INTEGRATION.md`, `OVERVIEW.md`.

- [x] **Bug fix / UI - title keypress lag before main menu:** `GameCoordinator` was built only after any-key on the title screen, causing a hitch. Warm construction runs during title idle (Background priority); keypress awaits warmup then shows the menu. Also dropped the redundant "Loading data..." flash on `ShowMainMenu` and pre-touches save listing. Tests: `TitleToMenuBootstrapTests`. Docs: `OVERVIEW.md`.

- [x] **Data / TIER 3 demo actions per TRIGGERS WHEN family:** Added heal samples for unused sheet WHENs (`HIT HEAL`, `CRIT HEAL`, `COMBO END HEAL`, `CRIT MISS HEAL`, `AFTER MISS HEAL`, `ROOM CLEAR HEAL`, `ROLL FIFTEEN`) so every `ActionTriggerSheetColumns.Families` token has a demo. Test: `ActionTriggerSheetColumnsTests`.

- [x] **Data / TIER 3 demo actions for new mechanics:** One Actions.json row per strip/retrigger/probability mechanic (+ NATURAL SEVEN for ONNATURALROLL); `ON NATURAL ROLL` trigger triple family on sheets.

- [x] **Combat / Balatro expressiveness #2 strip verbs:** Fight-scoped `StripMutationState` + trigger mechanics (`strip_jump`?`strip_replace_next`); `ComboRouter` disable/skip/shuffle/pending overrides. Tests: `StripMutationTests`.

- [x] **Combat / Balatro expressiveness #3 retrigger:** `retrigger_*` schedules nested full resolve (depth 1), distinct from Multihit. Tests: `RetriggerTests`.

- [x] **Combat / Balatro expressiveness #4 item WHEN bridge:** Weapon* DoTs use `ActionTriggerGate`; optional `Modification.triggerWhen`; target item grammar documented in OVERVIEW/ARCHITECTURE. Tests: `WeaponModTriggerBridgeTests`.

- [x] **Combat / Balatro expressiveness #10 probability content:** `ONNATURALROLL`, `salvage_miss`, `crit_face_min`, fixed `replace_next_roll` (not disadv), exploding dice mapping. Tests: `RollProbabilityContentTests`.

- [x] **Data / ACTIONS ? HERO/ENEMY BASE weapon columns:** Additional sheet columns **WEAPON SPEED** / **WEAPON DAMAGE** under `HERO BASE` / `ENEMY BASE` (also `? BASE STATS`). Flat cadence-scoped bonuses (`weaponSpeedMod` / `weaponDamageMod`); not ACTION SPEED/DAMAGE %. Combat apply via `WEAPON_SPEED` / `WEAPON_DAMAGE` deposits. Tests: `WeaponBaseModCadenceTests`, sheet serializer. Docs: `OVERVIEW.md`, `GOOGLE_SHEETS_INTEGRATION.md`.

- [x] **UI / Title screen accent piping dim:** Box-drawing outline glyphs on DEMON/FIGHTER use `IdlePipingBrightnessScale` (default **0.275**) so full-block `?` faces stay brighter than accent piping. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **Combat / Naivet? single-digit level fade:** NAIVETE starts at **3** (L1) and subtracts **1** per level (0 at L4+); hit steps are 1:1 with remaining points (max 3). Replaces the old attribute-sum remainder (tens-scale) formula. Tests: `NaiveteThresholdBonusesTests`. Docs: `OVERVIEW.md`.

- [x] **Combat / TRIGGERS bundle applicator:** On matching WHEN, apply only mechanics listed under that triple?s `?` cell. Blank SCOPE = instant; TURN/ACTION/FIGHT/DUNGEON = lasting grant via existing deposit APIs. Mechanics listed under any TRIGGERS ? are excluded from whole-row status / sheet-mod apply (`gate_only_listed`). WHEN with pointers does not merge into `triggerConditions`. Tests: `ActionTriggerBundleApplicatorTests`. Docs: `OVERVIEW.md` / `GOOGLE_SHEETS_INTEGRATION.md`.

- [x] **Data / Sheets ? CADENCES triples (replace K/L/M):** Compact `DURATION`+`CADENCE`+`MECHANICS` expanded to per-family triples under **CADENCES** (`TURN` / `TURN DURATION` / `TURN ?`, same for ACTION/FIGHT/DUNGEON). Magnitudes stay in detail columns. Push appends columns, writes triples, clears legacy compact cells. Pull prefers triples; legacy K/L/M still fallback. Converts into multi-block `ActionAttackBonuses` via `ActionCadenceEditorSync`. Tests: `ActionCadenceSheetColumnsTests`. Docs updated.

- [x] **Data / Sheets ? mechanic & cadence hover notes:** ACTIONS push refreshes **CADENCE_LIST** (full TURN / ACTION / FIGHT / DUNGEON rows) and **MECHANIC_LIST** (id + description + allowed cadences as full words) with Google Sheets cell notes; applies hover notes on ACTIONS header labels (TRIGGERS triples, CADENCE, DAMAGE vs DAMAGE MOD, etc.). Redundancy callouts where MECHANICS ids mirror detail columns. Tests: `ActionSheetAnnotationTests`. Docs: `GOOGLE_SHEETS_INTEGRATION.md` / `OVERVIEW.md`.

- [x] **Data / ACTIONS trigger pairs (3-column):** Spreadsheet TRIGGERS band uses per-family triples ? count (`ON KILL`), scope (`ON KILL SCOPE`, blank = instant), mechanic pointers (`ON KILL ?`). Magnitudes stay in existing mechanic columns. Pull parses pairs ? `ActionTriggerBundle` on `ActionData`; enabled WHEN tokens still merge into `triggerConditions` for the status gate. Push ensures missing pair columns on the live header and writes them. Tests + `GOOGLE_SHEETS_INTEGRATION.md` / `OVERVIEW.md`.

- [x] **Combat / Signature trigger hooks:** Wired List A moments via live gate + `CombatTriggerContext`: first blood (`ONFIRSTHIT`), after-miss (`ONAFTERMISS`), chain punctuation (`ONCOMBOEND`), clutch (`IFCLUTCH` / HP filters), mirror/switch-up (`IFSAMESACTION` / `IFDIFFERENTACTION`), last stand (`IFLASTENEMY`), under-DoT (`IFSOURCEUNDERDOT` / `IFTARGETUNDERDOT` + status tokens), tag synergy (`IFACTIONHASTAG` / `IFGEARHASTAG` / `IFTARGETHASTAG`). Supporting List C primitives in `ActionTriggerPredicates`. Settings checkboxes + status/tag/HP fields. Tests: `ActionTriggerGateTests`. Docs: `OVERVIEW.md`, `ARCHITECTURE.md`.

- [x] **Combat / Action trigger gates ? ONWIELD:** `ONWIELD:Sword|Dagger|Mace|Wand` filters by equipped weapon type (OR across types; AND with outcome tokens; wield-only ? connect). Settings On Wield checkboxes; Sheets parse via `TRIGGER CONDITIONS`; evaluator `IfWieldingWeaponType`. Tests: `ActionTriggerGateTests`, `ConditionalTriggersTests`. Docs: `OVERVIEW.md`, `ARCHITECTURE.md`, `GOOGLE_SHEETS_INTEGRATION.md`.

- [x] **Combat / Action trigger gates:** Wired `ONHIT`, `ONCONNECT`, `ONMISS`, `ONCRITICALMISS`, `ONCOMBO`, `ONCOMBOEND`, `ONCRITICAL`, `ONKILL`, `ONROLLVALUE`, `ONHEALTHTHRESHOLD`, `ONROOMSCLEARED` (+ optional `:N`) through `ActionTriggerGate` + hit/miss/kill/threshold/combo-end/room-clear paths; Sheets ingest `TRIGGER CONDITIONS` / `ON ROLL VALUE` / `ON ROOMS CLEARED`; Settings Actions form checkboxes + exact-roll / rooms-cleared fields. Tests: `ActionTriggerGateTests`. Docs: `OVERVIEW.md`, `ARCHITECTURE.md`, `GOOGLE_SHEETS_INTEGRATION.md`.

- [x] **UI / Title screen palette pacing:** Idle neon patterns hold **1s** then crossfade **1s** (`PaletteShiftIntervalMs` / `PaletteTransitionMs` = 1000). Docs: `OVERVIEW.md`.

- [x] **UI / Patches panel layout cleanup:** Settings ? Developer ? Patches split into Active selection / First-run default / Import-export / Active files sections; Apply+Refresh sit with selectors; paths use a labeled grid instead of a dense text dump.

- [x] **Settings / Shipped first-run balance default:** Tracked `GameData/ShippedPatchDefaults.json` sets which balance patch new installs get when `PatchProfile.json` is missing. Settings ? Patches **First-run default** + **Set shipped default**. First-run profile bootstrap + missing-active fallback use the shipped name (else `default`). Tests: `PatchProfileServiceTests`. Docs: `OVERVIEW.md`, `SETTINGS_SYSTEM_ARCHITECTURE.md`.

- [x] **Platform / Linux launcher:** Added `Dungeon Fighter(Linux).sh` (build + run; optional `--bg`), made `Scripts/install-dotnet.sh` work on Linux (apt + official installer), fixed `play.sh` / `play-automated.sh` hardcoded Windows paths. Docs: `LINUX_SETUP_GUIDE.md`, `README.md`, `OVERVIEW.md`.

- [x] **UI / Title screen black bg + palette crossfade:** Idle backdrop stays black; neon sequences hold then RGB-lerp over `PaletteTransitionMs` (1500). Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen vibrant complementary bg:** Backdrop uses `ComplementaryVibrantBackground` (high S/V neon fill) instead of the dark tint. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen neon palettes:** Added `neon_*` color codes and `title_neon_cyber|acid|plasma|hyper` templates; title idle picker prefers `title_neon_*` only. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen bg same phase as text:** Complementary backdrop shifts with the text palette (same interval/phase), not offset. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen bg 50% out of phase:** Complementary backdrop shifts on the same interval as text palettes but starts at half-period offset (independent accent palette). Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen full-bleed bg + sat +50%:** Complementary backdrop fills window letterbox (`SetShellBackgroundColor`); `IdleSaturationScale` 1.5; richer complementary tint. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen palette shift + complementary bg:** Idle color pattern changes every `PaletteShiftIntervalMs` (default 3s); canvas clear fill uses dark complementary of accent, reset to black on exit. Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen idle-first tweaks:** Boot skips fade?POP?settle and starts on random idle undulation; DEMON/FIGHTER colors use `IdleSaturationScale` 0.75 (?25%); press-key painted in same UI pass as each idle frame (no flicker). Tests: `TitleScreenAnimationTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen animation + random idle gradient:** Boot plays fade ? POP ? settle, then cancelable idle undulation via the same `dungeonSelection` `TextAnimationCompositor` + `DungeonSelectionAnimationState` as dungeon selection; per-launch random base palette (`TitleIdlePalettePicker`). Tests: `TitleScreenAnimationTests`, `TitleScreenAsciiSpacingTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Title screen ? no timed delay before any-key:** Removed the final hold / Main Title message delay on the boot title screen (`FinalHoldDuration` and `MainTitle` delay = 0). Zero-duration animation steps skip the min-frame pause so ?Press any key?? can appear immediately. Tests: `TitleScreenAsciiSpacingTests`; docs: `OVERVIEW.md`.

- [x] **UI / Flavor Text EXPAND auto-arrange:** On EXPAND, `FlavorTextWindowPlacement` sizes/places the four section windows from the Settings monitor working area with Settings (Generate) centered; left column Forms+Categories, right column Template+Legacy. Tests: `FlavorTextWindowPlacementTests`. Docs: `OVERVIEW.md`, `SETTINGS_UI_GUIDE.md`.

- [x] **UI / Flavor Text Generate-first + EXPAND/CONTRACT:** Generate section at top of Developer ? Flavor Text; **EXPAND** reparents Forms / Form template / Categories / Legacy banks into four non-modal windows; **CONTRACT** (or panel unload) re-docks. Docs: `OVERVIEW.md`, `SETTINGS_UI_GUIDE.md`.

- [x] **Settings / Flavor Text forms composer:** Developer panel authors forms with `<category>` placeholders, Generate at top, global shared category lists at bottom, Add Form/Category; legacy banks in collapsed expander. Persist `forms` + `categories` in `FlavorText.json` (authoring-only; not wired to live call sites yet). APIs: `ExpandFormTemplate` / `GenerateFormMany`. Tests: `FlavorTextBankCatalogTests`. Docs: `OVERVIEW.md`, `SETTINGS_UI_GUIDE.md`.

- [x] **Bug fix / Return to main menu after lab snapshot:** Game loop option **0** used `ConfigureAwait(false)` after async character save, so `ShowMainMenu` could run off the UI thread ? state became Main Menu (next **0** quit) while the screen stayed stuck. Also restore focus after the inventory snapshot dialog. Tests: `GameLoopInputHandlerTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action Lab ? typed dungeon seed:** Click cyan `[seed]` beside ?lvl to type an integer (replaces `[seed+]`); `SetLabDungeonSeed`. Docs: `OVERVIEW.md`. Verify: `dotnet run -- --run-test-filter ActionInteractionLab`.

- [x] **UI / Action Lab ? foes list fills catalog height:** Enemy type picker in `ActionLabCatalogWindow` uses remaining column height (same as the actions column) instead of a fixed 10-row cap; scroll uses `LastEnemyCatalogVisibleRowCount`. Docs: `OVERVIEW.md`. Verify: `dotnet run -- --run-test-filter ActionInteractionLab`.

- [x] **UI / Action Lab ? tools panel section spacing:** Blank row between Snapshots / Dungeon / turn / d20; footer flows under d20 pad (no bottom pin / middle dead space). `ActionLabControlsRenderer`. Docs: `OVERVIEW.md`. Verify: `dotnet build`.

- [x] **UI / Action Lab ? foes + actions in secondary window:** Keep Snapshots / Dungeon / turn / d20 / Step-Sim footer in the tools window; move foe type list + action catalog into `ActionLabCatalogWindow`. Open/refresh/close with tools; placement left of tools. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`. Verify: `dotnet build`.

- [x] **UI / Action Lab ? two-column tools layout:** Widen tools aux grid (~72?54) and default window (~920?960); left column = Snapshots / Dungeon / Foe / turn / d20; right column = action catalog; full-width footer for Step/Sim/Exit. Keep existing click tokens and wheel hit boxes. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`. Verify: `dotnet build` on `Code/Code.csproj`.

- [x] **Feature / Action Lab ? character snapshots + seeded dungeon tooling:** Named `GameData/LabSnapshots/` presets (stats/gear/combo strip) from Inventory + Settings ? Testing; lab Load; seeded `Dungeon.Generate` + room-by-room Step play; Seed d20 mode; **[ Dungeon Sim N ]** batch reports; wider tools window. Tests: `ActionInteractionLabTests` snapshot/seed/dungeon cases. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **Bug fix / Multihit shortfall on kill:** Multihit stopped summing ticks when the target hit 0 HP, so logs like `(4 hits) for 45` under-counted vs `(attack ? armor) ? N` (e.g. 88). Always resolve every planned tick; overkill clamps HP. Tests: `MultiHitTests.TestMultiHitFullDamageWhenTargetDiesMidSwing`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Combat log ? attack/armor footer clarity:** Roll detail `attack 23 - 1 armor` looked arbitrary next to multihit totals. Format as `attack: X - Y armor = Z` (and `? N` when multihit) via `DamageFormatter.AddAttackVsArmor` / `RollInfoFormatter` / plain `CombatResults`. Tests: `DamageFormatterTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`, `COMBAT_LOG_SPACING_STANDARD.md`.

- [x] **UI / Action strip ? ACTION grant stays on grantor:** Authored `ACTION (Nx)` / Multihit grant lines stay on the grantor (e.g. Rapid Strike); recipient (Slam) does not paint pending ACTION/Multihit text ? `Nx` damage + cyan shimmer carry the cue. `ACTION` is next-action wording only. Tests: `CombatActionStripBuilderTests.TestActionCadenceGrantLinesResetWhenPendingThenRedeemed`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action Lab ? sequence edit resets strip to slot 1:** Catalog add, strip right-click remove, and strip drag reorder call `ResetLabStripPositionToFirstSlot` (`ComboStep` = 0 + catalog sync). Tests: `ActionInteractionLabTests.LabSequenceEdit_ResetsStripPositionToFirstSlot`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **Bugfix / Action strip ? bank sticky after miss:** ACTION cadence additive bank (`2x`/multihit/shimmer) sticks to the intended recipient strip slot via `PendingActionCadencePreviewSlot` so miss/`ComboStep=0` no longer rebinds cues to the first card. Combat peek gates the same sticky slot. Tests: `CombatActionStripBuilderTests`, `ActionBonusBorderShimmerTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip ? amp baked into damage (no card amp: label):** Strip cards and compact tooltip `%` swing lines show **`N damage | X.Ys`** (no `| amp:`); Effective mode multiplies TECH slot amp + pending sheet `AMP_MOD` into the damage number. Hover keeps separate `AMP: ? = Pow(?)` calc. Combat log amp unchanged. Tests: `CombatActionStripBuilderTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Action strip ? show AMP on action info:** Combo-strip cards and hover tooltips include the resolved swing amp (`amp: 1.02x`) and a short TECH-baseline calculation line so slot amp is visible alongside damage/speed (same value combat uses). Tests: `CombatActionStripBuilderTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Action strip ? ACTION grant lines stay on grantor:** Authored `ACTION (Nx)` grant lines stay on the grantor while pending; recipient uses `Nx` damage + cyan shimmer (no relocated ACTION/Multihit text). Redeemed `ConsumedMultiHitMod` clears at end of `Execute` so Multihit/`2x` does not stick after Slam. Tests: `CombatActionStripBuilderTests` (grant stay), `MultiHitTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip ? longer bonus-border trail:** Traveling cyan highlight on buffed action cards covers ~? of the perimeter (not a 3-cell spark) so it reads as moving around the frame; always leaves a gap cell. `ActionBonusBorderShimmer.TravelHighlightFraction` / `GetTravelHighlightLength`. Tests: `ActionBonusBorderShimmerTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip ? shimmer on buffed cards only:** Cyan border shimmer (`ActionBonusBorderShimmer`) marks combo-strip slots that currently have pending ACTION-cadence buffs (slot queue / bank on current step), not cards that merely grant `ActionAttackBonuses`. Example: Rapid Strike does not shimmer; Slam does after Multihit is queued onto it. Tests: `ActionBonusBorderShimmerTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **Bugfix / Rapid Strike Multihit not self-applied:** ACTION cadence **MULTIHIT_MOD** (e.g. Rapid Strike) no longer inflates the granting swing?s combat-log hit count or deferred accuracy hit-layers. Hit count is captured as `ResolvedMultiHitCount` at damage time before the next-action bank deposit; formatting uses that value. Strip still shows +1 MH on the following sequential slot only. Tests: `MultiHitTests.TestActionCadenceMultiHitDoesNotApplyToGrantingAction`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **Bugfix / Rapid Strike MULTIHIT double-grant:** ACTION cadence next-action mods authored as both sheet columns (`multiHitMod`) and `ActionAttackBonuses` no longer stack. Sheet enqueue skips types already covered by ACTION bonus groups (bank is authoritative). Slam gets +1 MH ? 2 hits, not +2 ? 3. Tests: `MultiHitTests.TestActionCadenceMultiHitNotDoubleAppliedFromSheetAndBonuses`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip hover ? % damage/speed:** Action-slot hover tooltips show sheet `%` swing readouts (`Dmg 50% | Spd 100%`, multihit `2x50% damage`) again; strip cards keep calculated flat damage | seconds. Same base vs effective+amp toggle. Tests: `CombatActionStripBuilderTests`. Docs: `OVERVIEW.md`.

- [x] **UI / Actions ? Action set filters gameplay + Action Lab:** Settings ? Actions **Action set** (Through Tier X) persists as `GameSettings.ActionsActiveSetMaxTier` and gates gameplay + Action Lab via `ActionSetVisibility` (`GetAllActionNames` / `GetAction` / loot / gear / defaults / environments / lab catalog). Settings editor still loads full `GetAllActionData`. Tests: `ActionSetVisibilityTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Actions ? Cadence mechanic dropdown (Action-set style):** Cadence Mechanics mechanic ComboBox matches Action-set dropdown UX (placeholder, short labels, theme chrome). Timing-agnostic mechanics (`heal`, `disrupt`) appear for every cadence; current mechanic stays visible when cadence filter would exclude it. Tests: `ActionMechanicsRegistryTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip ? calculated damage and speed on cards:** Action strip cards show resolved values (e.g. `25 damage | 8.3s`); hover tip uses `%` damage/speed. Flat damage on cards uses hero base damage ? action/mod/amp percents (plus early-game / class mult); speed uses `GetTotalAttackSpeed ? Length` with pending SPEED_MOD. Toggle base vs effective+amp modes still color-codes diffs. Tests: `CombatActionStripBuilderTests`. Docs: `OVERVIEW.md`.

- [x] **Combat / Roll feel ? combo-band raw damage is 1.0?:** Rolls of **14+** (combo tier) no longer multiply raw damage by 1.5?. Combo power stays from strip **AMP**, action multipliers, and crit. Default + balance patch `comboRollDamageMultiplier` = **1.0**; variance-compression endpoints also 1.0. Tests: `DamageCalculatorTests.TestComboBandRollDoesNotAmplifyRawDamage`, `CombatConfigTests`, `RollFeelVarianceCompressionTests`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action strip ? bonus-card border shimmer:** Combo-strip cards with **pending** ACTION-cadence buffs (not merely authored `ActionAttackBonuses` grants) get a continuous cyan border shimmer (dual-sine color wave + traveling perimeter highlight). Selected next-slot cards shimmer white?cool cyan so the next cue stays readable; hit/miss/combo flash still overrides. `ActionBonusBorderShimmer.SlotHasPendingBonusCue`; tests `ActionBonusBorderShimmerTests`. Docs: `OVERVIEW.md`.

- [x] **Combat / ACTION cadence ? pending mods survive miss until combo:** Next-action bonuses (e.g. **MULTIHIT_MOD** from Rapid Strike) stay queued and update the strip until the follow-up is rolled; **miss / non-combo hit keep pending**; **hit+combo redeems**. `ResolvePendingActionCadenceBonuses`, strip peek via `GetEffectiveMultiHitCountForModifierScaling`. Tests: `ActionBonusMechanicsTests` (miss/non-combo keep), `MultiHitTests.TestActionCadenceMultiHitSurvivesMissUntilCombo`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **Bugfix / Item Generation ? affix slot dropdown appears stuck:** Shipped `perItemType` tables are identical across Head/Chest/Legs/Feet/Weapon, so switching the dropdown looked like a no-op. Hardened slot switch (`ItemAffixScratchBuilder`, deep-clone scratch, suppress SelectionChanged during load, resolve SelectedItem/index/AddedItems, wired panel ref + ?Showing table for:? label). Tests: `ItemConfigTests.TestItemAffixScratchBuilderDeepClonesPerSlot`, `TestItemAffixScratchBuilderResolvesSlotKey`. Docs: `PROBLEM_SOLUTIONS.md`, `OVERVIEW.md`.

- [x] **UI / Item Generation ? refresh stale affix-panel copy:** Help text and combo-cap tooltips now match live behavior: Extra slots axis included; `perItemType` primary with `perRarity` fallback (Weapon mirrored on Save); effective combo max includes gear ExtraActionSlots, class-tier upgrades, and Wand intrinsic bonus (`ComboSequenceMaxHelper`). Updated `LootSystemConfig` comments + `OVERVIEW.md`.

- [x] **Combat / Stun ? length = victim attack time:** Each stun turn skips one of the victim's attack-time periods (`GetTotalAttackSpeed`). Stun recovery uses `ActionSpeedSystem.AdvanceOwnTimeline` (same as `ExecuteAction`) so long env actions (e.g. Room Collapse 30s) no longer snap the stunned hero to global time and strand them for many foe catch-up swings. Removed `attackSpeed/10` duration tick. Tests: `StunProcessorTests`, `ActionSpeedSystemTests.TestAdvanceOwnTimeline`. Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`.

- [x] **UI / Action Lab ? F8 also opens Settings:** Main-menu **F8** starts Action Lab and opens the Settings pop-out so multi-window placement can left-anchor Settings beside the centered main window and right-docked lab tools. `StartActionInteractionLabAsync(..., alsoOpenSettingsWindow: true)` from `MainWindow`; Settings ? Testing path unchanged. Docs: `OVERVIEW.md`.

- [x] **UI / Settings ? Actions workshop set filter:** ACTIONS pull stamps `tier` from `TIER N ACTIONS` section markers onto each row (`SpreadsheetActionParser` ? `Actions.json` / `ActionData.Tier`). Settings ? Actions **Action set** dropdown filters the list to `tier ? X` (default max = show all). Extended so the same selection also gates gameplay and Action Lab (see **Action set filters gameplay + Action Lab**). Tests: `SpreadsheetActionDataSheetRowSerializerTests`, `ActionDataToSpreadsheetJsonConverterTests`, `ActionsTabManagerTests`, `ActionSetVisibilityTests`. Docs: `OVERVIEW.md`.

- [x] **Combat / Pierce ? ignore flat armor:** `CausesPierce` swings and pierced targets (`HasPierce` + `PierceTurns`) resolve armor as 0 in `DamageCalculator.ResolveTargetArmor` / `CalculateDamage`; combat roll footers pass the action through. `PierceTurns` decays in `Actor.UpdateTempEffects`. Mechanic registry marks pierce `onActions`. Tests: `DamageCalculatorTests.TestPierceIgnoresArmor`. Docs: `OVERVIEW.md`.

- [x] **Combat / Acid DoT (4th DoT):** Burn-style intensity + pending on the shared global poison/burn tick; each tick deals HP and adds full intensity to `AcidArmorReduction`. `ResolveTargetArmor` / hero `GetEffectiveArmor` subtract acid shred (enemies included). Crit-gated `weaponAcid` + `CausesAcid` / `AcidAmountToAdd`; sample **Caustic** mod; UI/log/health-bar channel; Expose unchanged. Tests: `CombatEffectsSimplifiedTests`, `DamageCalculatorTests`, `HealthBarDeltaDamageHintTests`, `StatusEffectsTests`, `ActorClearTempEffectsTests`. Docs: `OVERVIEW.md`.

- [x] **Reliability / Combat closeout (Phase 4):** `CombatManager.Cleanup()` on `ActionLabEncounterSimulator` ephemeral paths; `GameStateManager` clears legacy dungeon/room when registering/switching active character; `SettingsMenuHandler.SaveGameAsync` and `DeathScreenHandler` use `SaveCharacterAsync`; lab batch sim uses `QueueAsyncForcedD20Rolls` (parity with interactive lab). Tests + `COMBAT_RELIABILITY_PHASE4.md`; reliability track complete. Verification: filters `GameStateManagerMultiCharacter|SaveLoad|DeveloperSimMode|ActionInteractionLab|SettingsMenuHandler` pass; `dotnet build` succeeds.

- [x] **Reliability / Combat static bleed (Phase 3):** Interactive Action Lab uses `Dice.QueueAsyncForcedD20Rolls` (AsyncLocal) instead of process-global `SetTestRoll`. `DeveloperSimMode.BeginScope` scopes `NegativeHpFloor`; fundamentals runner uses scope instead of global assign. `HealthBarDeltaDamageHint` skips writes when muted; `CombatUiMuteScope.Begin` clears pending hints on mute enter. `ActionExecutor` last-action maps use `ConcurrentDictionary`. Action Lab session holds `GameTicker.BeginIsolatedEncounterGameTime` from `Begin` to `EndSession`. Tests + `COMBAT_RELIABILITY_PHASE3.md`; update `OVERVIEW.md` / `KNOWN_ISSUES.md` / `PROBLEM_SOLUTIONS.md`. Verification: filters `CombatUiMuteScope|HealthBarDeltaDamageHint|DeveloperSimMode|ActionInteractionLab|ActionExecutionFlow` pass; `dotnet build` succeeds.

- [x] **Reliability / Combat secondary nicks (Phase 2):** Log combat display failures in `BlockDisplayManager` (no silent swallow). Cancellable load + async menu-exit save in `CharacterSaveService` / `CharacterFileManager`. `GameStateManager` dungeon/room writes prefer active `CharacterContext` to stop legacy dual-write bleed on character switch. Muted/headless encounters wrap `GameTicker.BeginIsolatedEncounterGameTime` (`BattleExecutor`, `CombatSimulator`, live `RunCombat` when muted). Tests + `COMBAT_RELIABILITY_PHASE2.md`; update `OVERVIEW.md` / `KNOWN_ISSUES.md` / `PROBLEM_SOLUTIONS.md`. Verification: focused filters `CombatUiMuteScope|BlockDisplayManager|GameStateManagerMultiCharacter` pass; `dotnet build` succeeds.

- [x] **Combat / Armor ? flat persistent DR (not room pool):** Hero armor is flat damage reduction like enemy armor: subtract effective armor in `DamageCalculator`, do not consume or refill a pool each fight/room. `CharacterHealthManager.GetMaxArmor` remains the effective value (gear + fortify ? break/expose); `CurrentArmor` aliases that value. Remove depleting pool logic and redundant end-of-combat / room / equip refresh side effects for absorption. Update combat HUD (`Armor N`), roll footers (`attack X - Y armor`), tests (`CharacterHealthManagerTests`, `DamageCalculatorTests`). Docs: `OVERVIEW.md`, `PROBLEM_SOLUTIONS.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false` passes.

- [x] **Reliability / Combat display pacing + scoped mute (Phase 1):** GUI inter-line delays are real again (`CombatDelayManager.DelayAfterMessageAsync`); env turns await `DisplayActionBlockAsync`; fire-and-forget removed from combat batch sync path. `CombatUiMuteScope` + AsyncLocal `CombatEnvironmentContext`; `TurnManager.DisableCombatUIOutput` aliases `CombatManager`. Background dungeon + `RunCombat` accept `CancellationToken`; `BattleExecutor`/`RunCombat` call `Cleanup()`. Tests: `CombatDelayManagerTests`, `CombatUiMuteScopeTests`, `BackgroundDungeonTaskManagerTests`. Docs: `KNOWN_ISSUES.md`, `PROBLEM_SOLUTIONS.md`, `OVERVIEW.md`.

- [x] **Balance tuning profiles:** `GameData/TuningProfiles/*.json` define sim mode + validators/suggesters; `TUNESIM`/`TUNEANALYZE`/`TUNEAPPLY`; built-in profiles level-curve, midgame-balance, earlygame-balance, weapon-focus. Legacy LEVEL* commands preserved.

- [x] **UI / Settings menu overpass:** Reorganized flat 22-item sidebar into grouped navigation (Player Settings, Developer Settings, Balance & Tuning, Testing, About). Merged Text Delays + Text Animation ? **Text & Animation**; Item Prefixes + Suffixes ? **Item Affixes**; Action Lab launcher ? **Testing** tab. Renamed Import ? **Spreadsheet Import**. Deleted orphan `DifficultySettingsPanel` and `BattleStatisticsSettingsPanel`. Theme fixes for Text Animation presets and Enemy Tuning. Tests: `SettingsPanelCatalogTests`. Docs: `SETTINGS_SYSTEM_ARCHITECTURE.md`, `SETTINGS_UI_GUIDE.md`.

- [x] **Systems / Canonical taxonomy alignment:** Expanded tag registry to 77 tags (pool gates `item`/`action`, environment structure/activity tags, mechanic/roll tags, body-part flags). Removed Artificer archetype and `large`/`bulky` creature attrs; migrated `Enemies.json`, `Actions.json`, `Rooms.json`. Combat hooks: `ActionMechanicTagProcessor`, `ActionRollTagProcessor`, `EnvironmentRollModifier`, activity/unstable room mods. Tests: `TagDefinitionsTests`, `ActionMechanicTagProcessorTests`, `ActionRollTagProcessorTests`, `EnvironmentRollModifierTests`. Docs: `TAG_REGISTRY.md`.

- [x] **UI / Settings ? Combat tuning panel:** Settings ? **Combat tuning** replaces combat-related Game Variables categories with five layered expanders (HP/duration, win rate, roll feel, combo affordance, simulation goals), each knob wired to the config path combat reads. Includes **Run simulation** (weapon?enemy matchups, quality score, validation summary). `GameSettings.EnemyHealthMultiplier` now applies in `EnemyStatCalculator`. Tests: `CombatTuningParameterRegistryTests`, extended `PlayerTuningApplierTests`. Docs: `SETTINGS_SYSTEM_ARCHITECTURE.md`, `BALANCE_TESTING_PHILOSOPHY.md`.

- [x] **Systems / Tag registry implementation:** Canonical 58-tag vocabulary in `TagDefinitions.cs` (removed hero subclass tags; creature attrs: giant, large, young, tiny, bulky, frail, has_hands); `TagRegistry` seeded from definitions; 10 enemy archetypes on `archetype` field; `Enemy.Tags` via sheet + `apply-enemy-tags.py`; `RoomData.tags` ? `Environment.Tags` (ENVIRONMENTS sheet column + theme fallback); material prefix ? `Item.Tags`. Docs: `Documentation/TAG_REGISTRY.md`. Tests: `TagDefinitionsTests`, `EnvironmentTests` (theme fallback), `EnemyLoaderTests`.

- [x] **UI / Left panel ? AMP hover shows per-slot tiers:** Hovering **AMP** in STATS lists the combo-AMP damage multiplier for each strip slot (`Pow(TECH baseline, index)` for combo actions; non-combo slots show 1.00?). Empty strip previews up to the hero?s current max sequence length. Scope: `LeftPanelTooltipBuilder`, `LeftPanelTooltipBuilderTests`; docs: `OVERVIEW.md`. Verification: `dotnet build` on `Code/Code.csproj` and `LeftPanelTooltipBuilderTests` pass.

- [x] **Dungeon / Room search ? food and potions (not gear loot):** Post-combat room search picks a row from `Consumables.json` (PULL from Google Sheets **CONSUMABLES** tab, gid `828815998`, via `consumablesSheetUrl` in `SheetsConfig.json`; **PUSH** writes the same tab from `SheetsPushConfig.json` ? `consumablesSheetTabName` / `pushConsumablesTab`). `RoomSearchConsumableCatalog` loads definitions; `RoomSearchConsumableGenerator` resolves flat or `%` potency (food: % of max HP; stat potions: % of base `CharacterStats`; threshold potions: flat). Finds are **consumed immediately** in `RoomProcessor` via `SearchConsumableUseService.ApplyImmediately` (no inventory). Cleared on new run, completion, early exit, and clone-after-death. Trade-up still ignores `ItemType.Consumable`. Tests: `RoomSearchConsumableTests`, consumables CSV in `JsonArraySheetConverterTests`. Docs: `OVERVIEW.md`.

- [x] **World / Region travel ? 4d4 event count:** Each journey rolls **4d4** once to determine how many travel events (d20 route rolls) occur (4?16). `TravelRouteResult` stores the count and optional die faces; `RegionTravelHandler` and `GenerateRoute` iterate that many steps; the region travel UI shows the 4d4 line when dice are present. Tests use `scriptedEventCount` or `Dice.SetTestRoll` for stability. Docs: `OVERVIEW.md`, `TravelSettingsPanel.axaml`. Verification: travel logic and tests updated; full solution build may be blocked by unrelated Avalonia color errors in `DungeonRenderer.RoomAndCombat.cs` until those are fixed.

- [x] **World / Region travel ? d20-linear step pacing (Text Delays):** Each route step's reveal delay and its contribution to summary travel minutes scale with the **d20 value** (natural 20 uses base values only; each point below 20 adds configurable extra). Defaults are higher than the old fixed outcome tiers. Settings live in `GameData/TextDelayConfig.json` (`TravelRouteRollPacing`) and in **Settings ? Text Delays ? Region travel (per d20 roll)**. Travel time multiplier (Travel settings) still scales both. Scope: `TravelPacing`, `TravelModels`, `TextDelayConfiguration` / loader, Text Delays UI; tests: `TravelSystemTests`. Verification: `dotnet build "Code/Code.csproj"`.

- [x] **Bugfix / Windows launcher ? skip SDK install when present:** The PC launcher and `Scripts/install-dotnet.ps1` detect an installed .NET 8 SDK from `dotnet --list-sdks` and common Program Files dotnet locations before attempting installation. If found, the launcher skips the installer and continues to build/run with that dotnet path. Docs: `OVERVIEW.md`, `WINDOWS_SETUP_GUIDE.md`. Verification: PowerShell parser check for `Scripts/install-dotnet.ps1` passes; launcher batch reviewed without running the game.

- [x] **Bugfix / Death clone ? clear pre-death action slots:** Cloning after death now rebuilds the action pool without preserving the pre-death combo/action strip, so discarded gear does not leave old actions visible in slots until combat starts. Focused coverage: `CharacterCloneServiceTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-clone-strip-msbuild\" -p:BaseOutputPath="obj\agent-clone-strip-bin\"` passes.

- [x] **Bugfix / Text delays ? canonical combat log speed config:** Combat log action delay saves/loads from the tracked root `GameData/TextDelayConfig.json`, not the stale duplicate under `Code/GameData`. Shipped `ActionDelayMs` is `3000` so GitHub pulls use the same combat log pacing as local settings. Verification: `dotnet build "Code/Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj/agent-text-delay-config-msbuild/" -p:BaseOutputPath="obj/agent-text-delay-config-bin/"` passes.

- [x] **UI / Actions settings ? save path confirmation:** When the Actions settings tab saves `Actions.json`, show the exact path written so it is clear whether settings edits went to the repo `GameData/Actions.json` file that needs to be committed and pushed. Verification: `dotnet build "Code/Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj/agent-actions-save-path-msbuild/" -p:BaseOutputPath="obj/agent-actions-save-path-bin/"` passes.

- [x] **Repo hygiene / generated .NET artifacts:** Stop `Code/obj` MSBuild/NuGet intermediates from causing pull conflicts by making nested `bin/` and `obj/` ignore rules explicit, ignoring the generated NuGet/MSBuild intermediate filenames, and removing already-tracked generated artifacts from the Git index. Verification: `git check-ignore --no-index` matches the generated `Code/obj` files and `git ls-files "**/obj/*" "**/bin/*"` returns no tracked build artifacts.

- [x] **UI / Combat ? threshold chance delta colors:** In the THRESHOLDS percent view, chance values modified by shifted thresholds render green when the displayed percent is above the default chance, red when it is below default, and white when unchanged. Scope: `DiceRollThresholdRowsRenderer`, `ThresholdDisplayFormattingTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-threshold-chance-color-msbuild\" -p:BaseOutputPath="obj\agent-threshold-chance-color-bin\"` passes.

- [x] **UI / Combat ? threshold chance row order:** CHANCES mode now displays rows as **Crit**, **Combo**, **Hit**, **Miss**, then **Crit Miss**. Scope: `DiceRollThresholdRowsRenderer`, `ThresholdDisplayFormatting`, `ThresholdDisplayFormattingTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-threshold-chance-order-msbuild\" -p:BaseOutputPath="obj\agent-threshold-chance-order-bin\"` passes.

- [x] **UI / Combat log ? right-click copy:** Right-clicking inside the framed Avalonia combat log copies the full center display buffer as plain text, matching the existing Ctrl+C/Cmd+C behavior and ignoring the action strip, side panels, and settings/tuning overlays. Focused coverage: `CombatLogCopyInputTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-combat-log-copy-msbuild\" -p:BaseOutputPath="obj\agent-combat-log-copy-bin\"` passes.

- [x] **Bugfix / UI ? inventory sorted display numbers:** Sorted and filtered inventory views label visible bag rows as contiguous option numbers (`1, 2, 3, ...`) rather than original bag indices. Equip/discard selection translates the visible choice back to the item's real inventory index so actions still target the correct bag item. Focused coverage: `InventoryRendererTests`, `InventoryAutoEquipEmptySlotsTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-display-msbuild\" -p:BaseOutputPath="obj\agent-inventory-display-bin\"` passes.

- [x] **UI / Combat ? threshold panel chance toggle:** Clicking the visible THRESHOLDS row area switches the HUD between d20 threshold numbers and the percent chance for each exclusive d20 outcome (Crit, Combo, normal Hit, Crit Miss), then click again to return to numbers. Existing threshold math, queued HUD shifts, and section collapse behavior are preserved. Focused coverage: `ThresholdDisplayFormattingTests`, `StatsPanelStateManagerTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-threshold-chance-msbuild\" -p:BaseOutputPath="obj\agent-threshold-chance-bin\"` passes.

- [x] **UI / Combat ? speed notification placement:** Page Up/Page Down combat speed notifications render in the black space below the main game panels instead of using the canvas status message row. The speed ladder behavior is unchanged. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-speed-notice-msbuild\" -p:BaseOutputPath="obj\agent-speed-notice-bin\"` passes.

- [x] **UI / Combat ? Page Up/Page Down combat speed ladder:** Replaced the F11 developer fast-combat toggle with runtime Page Up/Page Down speed controls. Page Up steps combat pacing up through **1x, 2x, 5x, 20x**; Page Down steps back down. Existing combat/display delays scale by that multiplier instead of becoming an all-or-nothing instant mode. Focused coverage: `DeveloperModeStateTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-combat-speed-msbuild\" -p:BaseOutputPath="obj\agent-combat-speed-bin\"` passes.

- [x] **Bugfix / UI ? inventory sorted weapons group by type:** Inventory bag sorted views keep display-only behavior while grouping weapons by weapon type inside sorted results, so rarity and slot sorting no longer interleave Dagger/Sword/Wand/Mace rows by original inventory index alone. Focused coverage: `InventoryRendererTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-weapon-type-msbuild\" -p:BaseOutputPath="obj\agent-inventory-weapon-type-bin\"` passes.

- [x] **UI / Inventory ? bottom numpad shortcut hint:** The fixed bottom inventory action area now shows the numpad shortcuts for sorting (`+`), filtering unmet requirements (`-`), and equipping compatible items into empty gear slots (`*`). Focused coverage: `InventoryRendererTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-hint-msbuild\" -p:BaseOutputPath="obj\agent-inventory-hint-bin\"` passes.

- [x] **Bugfix / UI ? equip comparison hover placement:** On the Avalonia equip comparison prompt, item detail hover panels open away from the hovered comparison column so the current/new item stat text remains readable while inspecting the hover details. Focused coverage: shared hover placement helper plus existing inventory comparison hover IDs. Verification: `dotnet build "Code/Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj/agent-hover-placement-msbuild/" -p:BaseOutputPath="obj/agent-hover-placement-bin/"` passes.

- [x] **Bugfix / UI ? equip prompt keeps inventory sort and filter:** The equip item selection prompt reuses the main inventory screen's current bag sort mode and unmet-requirements filter. This originally kept original bag numbers as selections; the active sorted-display-number bugfix supersedes that by using contiguous visible choices while still mapping internally to real bag indices. Focused coverage: inventory handler selection flow plus inventory display entry mapping. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-selection-msbuild\" -p:BaseOutputPath="obj\agent-inventory-selection-bin\"` passes.

- [x] **UI / Inventory ? numpad sort and requirements filter:** Pressing numpad `+` on the main inventory screen cycles item display sorting between inventory order, rarity, and item slot. Pressing numpad `-` toggles hiding bag items whose STR/AGI/TEC/INT requirements the hero does not currently meet. The display preserves real inventory indices for equip/discard/hover paths. Focused coverage: key conversion and inventory display ordering/filter helper. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-sort-msbuild\" -p:BaseOutputPath="obj\agent-inventory-sort-bin\"` passes.

- [x] **UI / Inventory ? numpad auto-equip empty slots:** Pressing numpad `*` on the main inventory screen auto-equips empty equipment slots from the bag when the first compatible item for that slot meets the existing attribute requirement gate. It does not replace currently equipped gear, skips blocked items, and reports what was equipped. Focused coverage: `InventoryAutoEquipEmptySlotsTests`, `KeyInputConverterTests`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-auto-equip-msbuild\" -p:BaseOutputPath="obj\agent-auto-equip-bin\"` passes.

- [x] **Bugfix / UI ? inventory armor comparison colors:** Inventory bag rows and item-selection previews now compare armor values against the currently equipped same-slot armor item. Higher armor values render green, lower values render red, and equal values render white, matching equip comparison behavior. Focused UI coverage verifies same-slot baseline selection and lower inventory armor rendering red. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-armor-msbuild\" -p:BaseOutputPath="obj\agent-inventory-armor-bin\"` passes.

- [x] **Death / Clone revival option:** On the death screen, offer to clone the fallen hero. Choosing clone revives the same character into the game loop, discards equipped gear instead of returning it to the bag, advances the name suffix from **Jr.** to **III**, **IV**, **V**, etc. on repeated clone deaths, rebuilds unarmed actions, and saves the live clone. Declining keeps the existing dead-character tombstone flow. Focused coverage: `CharacterCloneServiceTests`, `DeathScreenHandlerTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-clone-msbuild\" -p:BaseOutputPath="obj\agent-clone-bin\"` passes.

- [x] **Bugfix / UI ? bleed DoT notification palettes:** Bleed damage-over-time combat log lines preserve the afflicted actor name palette through the colored-text markup round trip, and every `bleed` / `bleeding` status word in the DoT notification uses the shared bleeding status template rather than a generic error/red palette. Focused coverage: `DamageFormatterTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-bleed-palette-msbuild\" -p:BaseOutputPath="obj\agent-bleed-palette-bin\"` passes.

- [x] **UI / Action descriptions ? complete mechanical coverage:** Action hover tooltips and inventory action summaries now describe the runtime mechanics an action interacts with: target/type metadata, damage/speed, roll and threshold modifiers, trigger gating, status effects, combo routing, stat/accumulation rules, self-damage, and other advanced action fields. Focused regression coverage: `CombatActionStripBuilderTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-action-desc-msbuild\" -p:BaseOutputPath="obj\agent-action-desc-bin\"` passes.

- [x] **Bugfix / Equipment ? health-bonus prefixes affect max HP:** Prefix modifications such as **Sturdy** / **Ancient** with effect `HEALTH BONUS` now contribute their rolled value as flat equipment health, increasing `Character.GetEffectiveMaxHealth()` and triggering the existing current-health adjustment when equipped. Regression coverage: `EquipmentSystemTests`; docs: `OVERVIEW.md`. Verification: `dotnet restore "Code\Code.csproj" /p:BaseIntermediateOutputPath="obj\agent-health-bonus-msbuild\" /p:BaseOutputPath="obj\agent-health-bonus-bin\"; dotnet build "Code\Code.csproj" --no-restore /p:UseSharedCompilation=false /p:UseAppHost=false /p:BaseIntermediateOutputPath="obj\agent-health-bonus-msbuild\" /p:BaseOutputPath="obj\agent-health-bonus-bin\"` passes.

- [x] **Bugfix / Combat ? weapon basic released after class tier:** Required weapon basics (for example Mace **SLAM**) are compelled only while the hero is still below the first named tier for that weapon path. Once the corresponding class path reaches the first `ClassPresentationConfig` threshold, the action can be removed from the combo sequence and restore/rebuild paths no longer re-inject it as mandatory. Tests: `WeaponRequiredComboActionTests`; docs: `OVERVIEW.md`. Verification: `dotnet restore "Code/Code.csproj" /p:BaseIntermediateOutputPath="obj/agent-weapon-basic-msbuild/" /p:BaseOutputPath="obj/agent-weapon-basic-bin/"; dotnet build "Code/Code.csproj" --no-restore /p:UseSharedCompilation=false /p:BaseIntermediateOutputPath="obj/agent-weapon-basic-msbuild/" /p:BaseOutputPath="obj/agent-weapon-basic-bin/"` passes.

- [x] **Combat / INT AMP + TECH roll milestones:** Swapped attribute mechanics ? combo AMP uses `ComboAmplifierFromIntelligence` (`AMP = 1 + 0.5 * log10(INT + 1)` on effective INT); HIT/COMBO/CRIT milestone shifts use `TechniqueMilestoneThresholdBonuses` on effective TECH. Class primaries unchanged (Dagger?TEC, Wand?INT). Tests: `ComboAmplifierFromIntelligenceTests`, `TechniqueMilestoneThresholdBonusesTests`, updated selector/tooltip tests.

- [x] **Bugfix / UI - inventory item list scroll:** The Avalonia inventory items list now renders inside the space above the fixed bottom action menu. When the bag contains more rows than that region can fit, mouse wheel and keyboard scroll move the visible item window instead of letting item stats overlap the menu. Row hover details stay mapped to the original inventory indices; the active sorted-display-number bugfix changes player-facing row labels to visible-order numbers. Regression coverage: `InventoryRendererTests`; docs: `OVERVIEW.md`. Verification: `dotnet restore "Code\Code.csproj" /p:BaseIntermediateOutputPath="obj/agent-inventory-scroll-msbuild/" /p:BaseOutputPath="obj/agent-inventory-scroll-bin/"; dotnet build "Code\Code.csproj" --no-restore /p:UseSharedCompilation=false /p:BaseIntermediateOutputPath="obj/agent-inventory-scroll-msbuild/" /p:BaseOutputPath="obj/agent-inventory-scroll-bin/"` passes.

- [x] **UI / Hover tooltips ? readable paragraph spacing:** All Avalonia on-hover detail text now includes clear paragraph spacing so item, stat, action-strip, sequence, and action-pool hover panels are easier to scan. Spacing is applied through the shared tooltip line-building path rather than one-off fixes for a single item tooltip. Regression coverage: `LeftPanelTooltipBuilderTests`, `CombatActionStripBuilderTests`; docs: `OVERVIEW.md`. Verification: `dotnet restore "Code\Code.csproj" /p:BaseIntermediateOutputPath="obj\cursor-verify-msbuild\" /p:BaseOutputPath="obj\cursor-verify-bin\"; dotnet build "Code\Code.csproj" --no-restore /p:UseSharedCompilation=false /p:BaseIntermediateOutputPath="obj\cursor-verify-msbuild\" /p:BaseOutputPath="obj\cursor-verify-bin\"` passes.

- [x] **Tutorial / Training Ground ? scripted dice and narrative beats:** The pre-weapon Training Ground defines an ordered outcome sequence for the Training Dummy using **PUNCH HARD**: d20 **10** hit, **4** miss, **14** combo, **1** critical miss, and **20** crit combo finish. Each beat appends narrative copy to the same combat block, and the dummy has **13 HP** so the hit/combo beats soften it up before the final crit combo kills it. The script is created only for the Training Dummy tutorial, so normal dungeon combat remains random. Focused coverage added for the script sequence, PUNCH HARD unarmed action, and one-shot d20 queue. Docs: `OVERVIEW.md`. Verification: `dotnet build Code\Code.csproj --no-restore /p:UseSharedCompilation=false /p:OutDir="obj\agent-training-tutorial-build\"` passes.

- [x] **Bugfix / Combat log ? enemy surprise message spacing and color:** Enemy-first surprise narration after enemy stats should not insert a leading blank line before "You've been surprised!", and the line should be emitted as colored narration instead of plain white text. Regression coverage: `DungeonDisplayManagerTests`, `ColorSystemApplicationTests`; docs: `OVERVIEW.md`.

- [x] **UI / Stats hover ? base vs gear attribute breakdown:** Left-panel stat hover tooltips keep the existing equations while also showing each relevant attribute's base value, attribute-modified value before gear, and gear-added attribute value (catalog/material/suffix) so stats like Speed expose where the AGI input came from. Focused coverage added in `LeftPanelTooltipBuilderTests`; docs: `OVERVIEW.md`. Verification: `dotnet build Code\Code.csproj --no-restore /p:UseSharedCompilation=false /p:OutDir="obj\cursor-verify\"` passes.

- [x] **UI / Inventory ? equip comparison item hover details:** In the equip comparison prompt, hovering the current-item column or the new-item column now shows the existing detailed item tooltip overlay. Current gear resolves through the equipped slot (`lphover:gear:*`), and the candidate item resolves through its inventory row (`lphover:inv:*`) so Actions, requirements, stats, mods, and extra combo-slot details match the main inventory hover behavior. Regression coverage: `InventoryRendererTests`; verification: isolated `dotnet build Code\Code.csproj --no-restore /p:UseSharedCompilation=false /p:OutputPath="obj\agent-equip-hover-build\"` passes.

- [x] **Bugfix / Equipment ? material stat-name effects:** Material prefix rows such as **Gold** use designer-facing stat names (`STRENGTH` / `AGILITY` / `TECHNIQUE` / `INTELLIGENCE`) in `PrefixMaterialQuality.json`. Equipment stat calculation now treats those the same as the older internal `equipmentStr` / `equipmentAgi` / `equipmentTec` / `equipmentInt` keys so generated material bonuses affect effective STR/AGI/TEC/INT, not just item tooltip text. Focused regression coverage added for Gold ? +AGI. Verification: normal build was blocked by the running game locking `DF.exe` / `DF.dll` (pid 32892); isolated compile with `dotnet build "Code/Code.csproj" -p:UseAppHost=false -p:BaseOutputPath="bin_buildcheck\\" -p:BaseIntermediateOutputPath="obj_buildcheck\\"` passed.

- [x] **Bugfix / Combat log ? duplicate poison tick detail:** Poison/burn global DoT processing now only emits tick detail lines when that status actually ticks. Repeated DoT checks before the next tick interval should not add a standalone `(poison: N% of max HP)` line to the combat log. DoT damage-type words such as `poison` now use the shared status-effect color template instead of plain white text. Regression coverage added in `CombatEffectsSimplifiedTests` and `DamageFormatterTests`; docs: `OVERVIEW.md`. Verification note: compile is currently blocked by unrelated `PreWeaponPathIntroHandler` / `GameScreenCoordinator.ShowPreWeaponPathIntro` work.

- [x] **UI / Training Ground ? path choice intro screen:** After defeating the Training Dummy and before starter weapon selection, show a short interstitial line: "The quest to find yourself begins when you choose a path..." The line shimmers between warm white and cool white text, then any key continues to the existing weapon-selection flow. Tests: `PreWeaponPathIntroRendererTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / Training Ground skip ? path intro still appears:** Choosing **Skip tutorial** from the pre-weapon Training Ground offer now clears the pending tutorial flag and shows the same "The quest to find yourself begins when you choose a path..." interstitial before starter weapon selection. Regression coverage: `TrainingGroundOfferHandlerTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / Inventory ? equipped weapon action missing from pool:** Gear action resolution and action-pool rebuilds now treat any item in the weapon slot / with `ItemType.Weapon` as a weapon, even when it is deserialized or cloned as a base `Item` instead of `WeaponItem`, so item `Actions:` text (for example mace **SLAM**) matches the equipped action pool. Regression coverage verifies base weapon items resolve and load their required weapon basic.

- [x] **UI / Combat ? accelerated combat center-panel tint:** Runtime accelerated combat pacing repaints the framed center panel with a subtle dark blue-purple background while enabled, and returns it to black at normal speed. The tint is shared by normal layout renders and immediate speed changes from both the main and settings windows. Tests: `DeveloperModeStateTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / UI ? entity name brightness clamp:** Enemy and character name color segments should apply the same HSV brightness floor used by animated UI text, so dark creature palettes and class-name palettes stay readable on the black combat log/background. Tests: `AnimalEnemyNameColoredTextTests`, `HeroNamePanelColoredTextTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / UI ? room reward narration colors:** Post-room search messages should preserve structured item-name coloring for found loot, and dungeon heal messages should color healing keywords/amounts instead of rendering as plain white text. Tests: `ColorSystemApplicationTests`; docs: `OVERVIEW.md`.

- [x] **UI / Death sequence ? centered defeat summary:** Present the death summary as a cleaner centered block in the center log, with centered section headers, aligned statistic rows, preserved colored achievement lines, and the fixed footer still anchored at the bottom. Tests: `DeathScreenRendererTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / UI ? feet item action-slot info:** Feet item stat displays, including the equip comparison prompt, should show how many action slots the item adds (`Action slots: +N`) even when the value is zero. Keep positive extra-slot support for any gear that can roll `ExtraActionSlots`. Tests: `ItemStatFormatterTests`; docs: `OVERVIEW.md`.

- [x] **UI / Dungeon completion ? centered victory summary:** Present the dungeon completion summary as a cleaner centered block in the center log, omitting redundant dungeon/health detail rows while keeping centered headers, aligned stat/reward rows, and existing level-up/loot coloring preserved. Tests: `DungeonCompletionRendererTests`; docs: `OVERVIEW.md`.

- [x] **Progression / Class upgrade action slots:** Crossing a class tier threshold on any weapon path grants +1 effective combo/action strip slot, added to the existing gear-based `ExtraActionSlots` capacity and still capped by `LootSystemConfig.ComboSequenceAbsoluteMax`. Level-up feedback calls out newly gained slots. Tests: `ComboSequenceMaxHelperTests`, `LevelUpSystemTests`; docs: `OVERVIEW.md`.

- [x] **Progression / Wizard weapon combo slot:** Any equipped Wand (Wizard class weapon) grants +1 combo strip slot via `ClassPresentationConfig.WandEquippedComboSlotBonus`, included in `EquipmentBonusCalculator` `ExtraActionSlots` totals and weapon item stats/tooltips. Tests: `ComboSequenceMaxHelperTests`, `ItemStatFormatterTests`; docs: `OVERVIEW.md`.

- [x] **Settings / Starter gear and weapon action rules visibility:** Items settings rows now show **Starter:** and weapon **Actions:** summaries, and the edit dialog has a **Starter** checkbox that writes the `starter` tag. Actions settings exposes **Required Weapon Basic** next to weapon-type assignment so `weapon_basic` + `weaponTypes` rules are visible/editable; canonical basics are now seeded in `Actions.json` (`STRIKE` Sword, `STAB` Dagger, `SLAM` Mace, `CAST` Wand). Cleared the duplicate Wand **Stick** starter tag so settings shows one starter per weapon type. Tests: `GearActionNamesTests`, `ItemsDataServiceTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / Combat ? enemy special actions bypass roll thresholds:** Lich-style enemies with only combo-flagged action-pool specials (e.g. **CRITICAL ATTACK**, **STUN**) now use the unnamed normal attack on rolls below the enemy combo threshold, even when no explicit combo sequence was built. Enemy action selection uses combo-flagged action-pool entries as the enemy combo strip fallback; regression coverage in `ActionSelectorRollBasedTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / Loot ? armor affixes bypass Actions chance table:** Non-action armor affixes (prefix/stat suffixes) no longer synthesize random granted actions through `GearActionNames`, and rolled armor no longer receives a random `GearAction` in `LootItemSelector`. Random granted-action lines now come from explicit `GearAction` or rolled `ActionBonuses` governed by the Item Generation **Actions** +%/Min/Max table. Tests: `GearActionNamesTests`, `LootItemSelectorTests`; docs: `OVERVIEW.md`.

- [x] **Bugfix / UI ? dungeon exit prompt divider:** The post-room exit choice prompt now draws the same cyan `====================================` divider above the `1 - Stay and continue through the dungeon` option that it already draws below the two choices, while the room-cleared message no longer draws a separate plain divider immediately before it. A blank line now separates preceding room narrative (search / consumable use) from the top divider. Tests: `DungeonExitChoiceHandlerTests.TestBuildExitChoiceMenuLines_IncludesTopAndBottomDividers`, `CombatMessageHandlerTests.TestAddRoomClearedMessage_DoesNotAddDivider`.

- [ ] **Audio / Cross-platform audio cues and menu music:** New `Code/Audio/` module on top of **SoundFlow** (MIT NuGet, MiniAudio backend, runs on Windows / macOS / Linux without extra installs). Splits **music** (one streaming track, crossfaded on `GameStateManager.StateChanged`) from **SFX** (short, cached, fire-and-forget). `AudioCue` enum names every trigger; `GameData/Audio/AudioConfig.json` maps cue ? file (+ volume / optional `rateLimitMs`) and `GameState` ? music cue. `ActionEventPublisher` now fires the five direct combat outcome cues (`Combat_CriticalMiss`, `Combat_Miss`, `Combat_Hit`, `Combat_ComboComplete`, `Combat_CriticalHit`) through `AudioCues.Trigger(...)` immediately after publishing hit/miss events, while `AudioCueDispatcher` still subscribes to non-outcome combat events (`EnemyDied`, `HeroLowHealth`, `EnemyLowHealth`, `StatusEffectApplied`) and rate-limits chatty cues; `MusicController` crossfades on state changes. Static `AudioCues.Trigger(cue)` facade fires from `MenuCommand`, `MainMenuHandler` invalid-input branch, `MainWindowInputHandler` Esc/Back, `LevelUpManager`, `EquipmentManager` equip/unequip, `InventoryManager` pickup, `DungeonDisplayManager.StartDungeon`, room clear, and `CharacterSaveManager` save success. New **Settings ? Audio** tab edits `AudioConfig.json` live (volume sliders + mutes, state?music dropdowns, per-cue Browse / volume / Test / Clear). Engine abstraction `IAudioEngine` with `SoundFlowAudioEngine` production backend and `NullAudioEngine` for tests / headless. SFX mixer work posts to `Dispatcher.UIThread` so combat-thread SFX can play on the Avalonia-owned audio backend. Tests in `Code/Tests/Unit/Audio/AudioCueDispatcherTests.cs` cover combat publisher routing, music state transitions, rate-limit, missing-file tolerance, and global-mute respect. Docs: `OVERVIEW.md` (Audio ? cues, music, and the Audio settings tab).

- [x] **Bugfix / Audio ? music mute should not mute SFX:** Moved the Audio tab master mute state into `AudioConfig.masterEnabled` instead of the legacy `GameSettings.EnableSoundEffects` flag. `AudioCueDispatcher` and `MusicController` now gate playback through `AudioConfig.masterEnabled` plus the specific bus flag (`musicEnabled` / `sfxEnabled`), so muting music only stops music while combat/menu SFX still play. Regression coverage: `AudioCueDispatcherTests.TestMusicBusDisableSilencesMusicOnly` and `TestMasterDisableSilencesBothBuses`; docs: `OVERVIEW.md`.

- [x] **Bugfix / Audio ? outgoing music loops through fade:** When a music track is crossfading out and reaches its natural end before the fade timer completes, restart that outgoing player at the beginning and continue the fade so the transition never drops to silence. Focused regression coverage added for the fade-loop restart policy; docs: `OVERVIEW.md`. Verification note: `dotnet msbuild Code\Code.csproj /t:CoreCompile /p:UseSharedCompilation=false` passes; full `dotnet build` is blocked by the currently running `DF.exe` process locking `Code\bin\Debug\net8.0\DF.exe` / `DF.dll`.
- [x] **Bugfix / Audio ? first music cue uses crossfade fade-in:** When a music cue starts while no music is currently playing, the incoming track now starts at zero volume and fades in over the configured `musicCrossfadeMs`, matching the crossfade length used for track-to-track transitions. Focused regression coverage: `AudioCueDispatcherTests.TestMusicFadeIncomingTrackPolicy`; docs: `OVERVIEW.md`.
- [x] **Bugfix / Audio ? SoundFlow EOF recursion crash:** Music players now keep SoundFlow `IsLooping` disabled and restart ended tracks from a posted mixer action after `PlaybackEnded`, avoiding recursive `SoundPlayerBase.HandleEndOfStream` calls when a track reaches EOF during looping or long crossfades. Focused regression coverage: `AudioCueDispatcherTests.TestMusicFadeLoopRestartPolicy`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-audio-eof-msbuild\" -p:BaseOutputPath="obj\agent-audio-eof-bin\"` passes.

- [x] **Audio / Combat directional hit cues:** Successful enemy attacks against the hero now fire `Combat_HeroHurt` instead of the generic hero-offense cues (`Combat_Hit`, `Combat_ComboComplete`, `Combat_CriticalHit`), so SFX can distinguish **enemy hits you** from **you hit the enemy**. Misses remain on the shared miss cues. `GameData/Audio/AudioConfig.json` binds `Combat_HeroHurt` to `SFX\hitHurt (1).wav` with the same 80 ms rate limit as generic hits. Regression coverage: `AudioCueDispatcherTests.TestPublisherRoutesEnemyHitToHeroHurtCue`; docs: `OVERVIEW.md`.

- [x] **Audio / Combat low-health cues:** Added `Combat_HeroLowHealth` and `Combat_EnemyLowHealth` cues. Combat publishes `HeroLowHealth` / `EnemyLowHealth` events when action damage or poison/burn/bleed ticks move a living actor from above 20% HP to at-or-below 20% HP; dispatcher routes each to its own SFX cue. Tests: `AudioCueDispatcherTests` low-health routing + threshold crossing. Docs/config: `OVERVIEW.md`, `GameData/Audio/AudioConfig.json`.

- [x] **Bugfix / UI — inventory pool action requirements and item hover:** Right-panel **INVENTORY (from bag)** action rows omit actions from bag items whose attribute requirements the hero does not meet. Hovering a visible bag action row shows the item detail hover for the item that grants that action, so duplicate action names can be traced to their source item. Tests: `InventoryActionPoolEntriesTests`, `RightPanelActionHoverStateTests`, `InventoryRightPanelLayoutTests`; docs: `OVERVIEW.md`. Verification: `dotnet build "Code\Code.csproj" -p:UseSharedCompilation=false -p:UseAppHost=false -p:BaseIntermediateOutputPath="obj\agent-inventory-pool-msbuild\" -p:BaseOutputPath="obj\agent-inventory-pool-bin\"` passes.
- [x] **Bugfix / UI — inventory bag action hover tooltip wiped by strip overlay:** Hovering **INVENTORY (from bag)** rows drew the item tooltip in the right panel, then `RenderActionInfoStrip` cleared the center overlay (early-out only covered gear **POOL**), leaving an empty center and orphan fragments (e.g. `×0.75)`). Skip strip tooltip clear for `HoveredInventoryPoolIndex`, clamp bag-item tips to the center panel, and sync `InventoryRightPanelLayout` row Y with the renderer. Tests: `InventoryRightPanelLayoutTests`; docs: `OVERVIEW.md`.
- [x] **Bugfix / UI — inventory bag action rows are unique:** The right-panel **INVENTORY (from bag)** action list collapses duplicate action names across compatible bag items, keeping the first matching bag item as the click/equip target. The equipped **POOL (from gear)** remains unchanged, so duplicates still appear when multiple equipped items grant the same action. Focused coverage: `InventoryActionPoolEntriesTests`; docs: `OVERVIEW.md`.

- [x] **Data / Suffixes ? bracketed `Requirements` column (multi-stat equip gate):** SUFFIXES sheet / `StatBonuses.json` rows accept an optional **`Requirements`** cell in the same `[key:value,?]` bracket format as `Mechanics`. May list one or more entries; **duplicates take max** (`[strength:5,strength:4]` ? strength 5). Keys are the four core attributes plus the four dynamic categories (`primary` / `secondary` / `neglected` / `weakness`) which resolve at equip-check time through `DynamicAttributeCategoryResolver`. Sheet headers `Stat Requirement` / `Requirement` / `Requirements` import onto `StatBonus.Requirements`; the legacy `Stat Requirement` ? `ItemRank` alias was retired (catalog rarity filter is authored in the **`ItemRank`** column). `LootBonusApplier` clones requirements onto the rolled suffix; `Item.RecomputeAttributeRequirementsIncludingModifications` merges them with prefix + catalog gates (max per stat). Tests: `JsonArraySheetConverterTests.TestStatBonusesRequirementsBracketImport`, `ItemTests.TestRecomputeIncludesSuffixRequirements` + `TestEquipBlockResolvesDynamicSuffixCategory`, `ItemSuffixesDataCoordinatorTests` round-trip. `OVERVIEW.md` (Data / Google Sheets ? item suffixes).

- [x] **Data / PREFIX sheet A?I + prefix attribute gates:** Prefix tab uses columns A?I (`DiceResult`, `ItemRank`, `Name`, `Description`, `Effect`, `value`, `prefixCategory`, `ATTRIBUTE REQUIREMENT`, `REQUIREMENT VALUE`); CSV import + JSON normalizer; `Modification.attributeRequirements`; `Item.CatalogAttributeRequirements` + `RecomputeAttributeRequirementsIncludingModifications` for loot and Action Lab; `JsonArraySheetConverterTests`, `ItemTests`; `OVERVIEW.md`.

- [x] **Character / Combat ? dynamic attribute categories (internal):** Rank effective STR/AGI/TECH/INT as PRIMARY / SECONDARY / NEGLECTED / WEAKNESS (tie-break STR?AGI?TECH?INT). Resolve at use time in `CharacterFacade.ApplyStatBonus` and `ActionExecutionFlow` stat-bonus paths so action data can target e.g. **PRIMARY** without UI surfacing. `DynamicAttributeCategoryResolver`; tests in `CharacterAttributesTests`, `ActionBonusMechanicsTests`; `OVERVIEW.md`.

- [x] **UI / Combat ? strip flash colors (hit vs combo):** Miss = pulsing **red** (same blink cadence as hits); normal hit = pulsing **green**; **gold** only for multi-step combo completion (last strip slot or finisher tag). `HeroActionStripFlashKind`, `ActionExecutionFlow.ShouldFlashComboComplete`, `HeroActionStripFeedback`; settings copy in `GameplaySettingsPanel.axaml`; tests `HeroActionStripFeedbackTests`; `OVERVIEW.md`.

- [x] **UI / Weapon selection ? weapon name uses rarity color:** Starting weapon selection menu draws the weapon **name** in the item rarity color (Common = **white**, Uncommon = green, Rare = blue, etc.) instead of forcing white/yellow. `WeaponSelectionRenderer.GetWeaponNameColor`; tests `WeaponSelectionRendererTests`.

- [x] **Bugfix / UI ? Wizard missing from weapon selection:** Restore the Wand starter catalog tag so new-game weapon selection shows all four class paths (Barbarian, Warrior, Rogue, Wizard), and add regression coverage that the starter menu includes one row for each class weapon type when starter tags are present.

- [x] **UI / Game menu ? prompt text style + centering:** Render ?What would you like to do?? as normal **white** text centered above the in-game menu options (Go to Dungeon / Show Inventory / Save & Exit), instead of the gold ASCII header.

- [x] **UI / Action strip + tooltips ? compact mechanics:** `CombatActionStripBuilder` / `DungeonRenderer` omit **Weapon basic** reminder text, **Type \| Target \| combo** metadata, and **Ability**-cadence `ActionAttackBonuses` lines on strip cards and hover tooltips (required basic is still indicated by **red** strip name + `WeaponRequiredComboAction` rules). **Opener** / **Finisher** lines remain. Tests: `CombatActionStripBuilderTests`; `OVERVIEW.md` (inventory bullet).

- [x] **Bugfix / Training Ground ? unarmed opener shows JAB:** The pre-weapon tutorial uses the shipped **PUNCH HARD** row (100% damage/speed) as its unarmed opener, with `CharacterSerializer` and `ComboSequenceManager` preferring `GameConstants.TrainingGroundTutorialActionName` after rebuild. Tests: `RebuildCharacterActionsComboTests`; `OVERVIEW.md` (Training Ground unarmed baseline).

- [x] **UI / Combat ? combo strip border feedback:** Next combo-slot panel uses a **white** border; miss / hit / combo-complete use pulsing borders (**red** / **green** / **gold**) with configurable durations (`GameSettings` + **Settings ? Gameplay**). Tests: `HeroActionStripFeedbackTests`, `ActionInfoStripLayoutTests`, `SettingsSaveRevampTests`; `OVERVIEW.md`.

- [x] **Bugfix / UI ? combat appears stuck after first strike / Training Ground freeze:** `DisplayTiming.ScheduleRender` and `ForceRender` must not invoke render callbacks while holding `timingLock` (nested scheduling from a render deadlocks the UI thread). Debounced timer path must also invoke outside the lock. Combat runs on a threadpool thread: `HeroActionStripFeedback.Trigger` marshals `DispatcherTimer` + canvas invalidate to the UI thread. Tests: `DisplayTimingTests`, `HeroActionStripFeedbackTests`; `OVERVIEW.md`.

- [x] **Bugfix / New game ? five starter weapons:** Two identical Wand **Stick** rows were both tagged **starter** in `Weapons.json`, so the menu showed five entries. Removed **starter** from the duplicate row; `StarterCatalogItems.ResolveStarterWeaponMenuCatalogRows` now keeps **one row per weapon type** after class-path sort. Tests: `StarterCatalogItemsTests`; `OVERVIEW.md`.

- [x] **UI / Combat log ? mouse wheel scroll:** `MainWindow` wires `PointerWheelChanged` on `GameCanvasHitSurface` to `MouseInteractionHandler.HandlePointerWheelChanged`; scroll applies only when the pointer is over the framed combat log (`LayoutConstants.ContainsCenterPanelContent`). `OVERVIEW.md`; `LayoutConstantsCenterPanelHitTests` doc note.

- [x] **Onboarding / Training Ground (pre-weapon):** Offer Training Ground or skip before starter weapon; tutorial combat **without** equipped weapon (rebuild action pool / fallbacks); completion routes to weapon selection without completion XP/loot; tutorial death revives to offer; save flag `pendingPreWeaponTrainingGround`; tests `DungeonTests.TestTrainingGroundDungeonGenerate`, `SaveLoadSystemTests.TestPendingPreWeaponTrainingFlagRoundTrip`, `RebuildCharacterActionsComboTests` no-weapon pool; `OVERVIEW.md`.

- [x] **Data / Equipment ? attribute requirements on equip:** Catalog `attributeRequirements` on weapons/armor (sheet abbrev + `requirement value` normalized in `GameDataJsonNormalizer`) must be met by effective STR/AGI/TEC/INT to equip. `EquipmentManager.TryEquipItem` / `Character.TryEquipItem`; inventory compare + confirm + combo pool paths; console `InventoryOperations`; `GameInitializer` logs if starter data ever mismatches; Action Lab `TryApplyLabGear` + `MouseInteractionHandler` message strip. UI **Requires:** line in `ItemStatsFormatter` + `LeftPanelTooltipBuilder`. Tests: `EquipmentManagerTests` (block/succeed), `LeftPanelTooltipBuilderTests` (tooltip line).

- [x] **Data / Equipment ? LEGS armor slot (full stack):** Fifth armor slot `legs` in `Armor.json` ? `LegsItem` / `ItemType.Legs`; equipment + save/load + HUD/inventory/Action Lab/Item Generation lab; optional catalog **`attackSpeed`** ? **`CatalogAttackSpeed`** ? equipment **`AttackSpeed`** bonus (`EquipmentBonusCalculator`, `SpeedCalculator`); `ArmorGenerator` per-tier `legs` baselines; `GameDataJsonNormalizer` coerces armor **`attackSpeed`**; loot **`LootItemSelector`**: **50% weapon**, **12.5%** each **head / chest / legs / feet** then slot-filtered row pick. Tests: `ItemGeneratorTests`, `SaveLoadSystemTests`, `SpeedCalculatorTests`, `LootItemSelectorTests`, `StarterCatalogItemsTests`, `ItemGenerationLabServiceTests`; `OVERVIEW.md`.

- [x] **Bugfix / Settings ? Item Generation Feet (per-slot affix) not persisting:** `GameVariablesTabManager` saved via `VariableEditor` which cached the first `GameConfiguration` instance; after `SaveChanges` called `ResetInstance()`, further saves wrote that orphaned object and wiped `itemAffixByRarity.perItemType` (e.g. Feet). Fixed by binding all variable lambdas to `GameConfiguration.Instance`. Settings **Save** now resolves the Item Generation panel when it is visible; affix slot `ComboBox` re-selects after `ItemsSource` refresh. Test: `VariableEditorTests.TestGameVariableBindingUsesLiveConfigurationInstance`.

- [x] **UI / Item Generation ? affix + lab strip uses width:** Top ?Real loot? row: outer strip scrolls **vertically only**; the **affix** card sits in a **`*`** grid column with **horizontal** scroll **disabled** on that strip?s `ScrollViewer` so the matrix gets a bounded width and **`*`** columns shrink to fit (no horizontal scrollbar); affix `TextBox` cells use **`MinWidth="0"`**; tighter rarity column + gutter. The **340px** lab **rarity %** + **tier %** column stays visible at right. Inner table `ScrollViewer` (max height) keeps vertical scroll for the matrix only.

- [x] **UI / loot tuning ? Item Generation affix grid: extra strip slots:** Fourth column group **Extra slots** (+%, Min, Max) per rarity and per slot (Head/Chest/Feet/Weapon) for `extraComboSlots` / `extraComboSlotsMax` / `extraComboSlotsExtraChance` on `ItemAffixPerRarityEntry`; `LootBonusApplier` adds rolled slots to `Item.ExtraActionSlots` (after catalog roll). `ItemAffixRollRule` + `RollAffixCounts` extended; tests `ItemConfigTests.TestItemAffixExtraComboSlotsJson`, `LootBonusApplierTests.TestLootAffixRollsExtraComboSlotsOntoItem`; `OVERVIEW.md`.

- [x] **UI / combat ? creature enemy names multi-shade:** Animal-keyword enemy names use per-glyph color palettes (`AnimalEnemyNameColoredText`); wired through `EntityColorHelper` append helpers, `RightPanelRenderer` + `ColoredTextWriter`, combat/damage/status builders, encounter strings. Tests `AnimalEnemyNameColoredTextTests`; `OVERVIEW.md`.

- [x] **UI / Item names ? narrative colors + pipeline cleanup:** Composed gear names use one formatter path (`ItemNameFormatter`, wired through `ItemColorSystem.FormatFullItemName` / `FormatSimpleItemDisplay`). **Quality** prefixes ? craftsmanship bronze tone; **Adjective** ? template sequences when present; **Material** ? substance palette / templates / silver fallback; suffix **`of` / `of the`** connectors ? dim gray; **stat bonus** keywords ? same rarity **color ramp** as gear tier (`StatBonus.Rarity`, not shadowed by word templates); modification suffix keywords ? `Modification.ItemRank` themes. `ItemThemeProvider` centralizes roles; `ItemThemeFormatter` delegates name lines; `ItemNameParser.RemoveRarityPrefix` strips Mythic / Transcendent. Tests: `ColorSystemApplicationTests` (stat suffix tier, Mythic strip, `ItemColorSystem` parity).

- [x] **UI / action strip beyond combo cap:** Top-center action-info panels at indices ? effective max combo slots (`ComboSequenceMaxHelper`) use black borders; pointer hover ignores those cells. `ActionInfoStripLayout.GetPanelBorderColor`, `DungeonRenderer.RenderActionInfoStrip`, `MouseInteractionHandler`; tests `ActionInfoStripLayoutTests`; `OVERVIEW.md`.

- [x] **Loot / combo extra slots (gear-wide + catalog min/max):** `EquipmentBonusCalculator` maps catalog `Item.ExtraActionSlots` into `GetStatBonus("ExtraActionSlots")` for all equipped slots; `ComboSequenceMaxHelper` uses that sum (still clamped by `ComboSequenceAbsoluteMax`). `ArmorData` / `WeaponData` gain optional `extraActionSlotsMin` / `extraActionSlotsMax` (JSON) with one-time roll in `ItemGenerator.RollCatalogExtraActionSlots` when either bound is non-zero after clamp/swap; legacy `extraActionSlots` when min/max unused. Sheet canonical headers updated; tests (`ItemGeneratorTests`, `ComboSequenceMaxHelperTests`); Settings combo-cap tooltips; `OVERVIEW.md`.

- [x] **Bugfix / UI ? inventory Actions:** Duplicate action names on bag rows (e.g. **Tight Combo, Tight Combo**) came from legacy armor-extra action handling after **`Resolve`** already prepended armor **`GearAction`**. Fixed by not appending a second copy; follow-up loot fix now also removes hidden random armor actions so only explicit **`GearAction`** and rolled **`ActionBonuses`** contribute action names. Test **`TestArmorExplicitGearAction_NotDuplicatedWhenSpecialMods`** in **`GearActionNamesTests`**. **`OVERVIEW.md`** (inventory bullet).
- [x] **Bugfix / Combat log ? queued action mods show immediately:** When an action queues next-roll modifiers (**`SPEED_MOD` / `DAMAGE_MOD` / `MULTIHIT_MOD` / `AMP_MOD`**), display a log line immediately after the action (instead of only showing the effect later when the modifiers are consumed on the following roll). Implemented in `ActionStatusEffectApplier.AppendQueuedActionModMessages` and wired through `ActionExecutor.ExecuteActionWithStatusEffectsColored`. Test `CombatResultsTests.TestQueuedActionModsShowImmediatelyAfterQueuingAction`.

- [x] **Combat / Unarmed ? default tutorial action:** When the hero has **no equipped weapon**, the default tutorial combo/action is **PUNCH HARD** (100% damage, 100% speed) so Training Ground has a consistent baseline attack. Code: `CharacterSerializer.EnsureActionPoolNotEmptyAfterRebuild`, `ComboSequenceManager.InitializeDefaultCombo`. Tests: `RebuildCharacterActionsComboTests`.

- [x] **Bugfix / Combat ? combo bonus not applied:** Applied persistent `ComboBonus` + temporary `TempComboBonus` as a **combo-threshold reduction** in both selection preview (`ActionSelector`) and combat resolution (`ActionExecutionFlow`, `AttackActionExecutor`), so combo chance matches the configured bonus. Added regression test `ActionExecutionFlowTests.TestComboBonusLowersComboThresholdForResolution`. *(Resolution path must call `AdjustComboThreshold(..., bonus)` with a **positive** `bonus` so the threshold is lowered; a negative argument raises it.)*

- [x] **Bugfix / Equipment ? percent suffixes have a minimum visible bonus:** `StatBonuses.json` / `Item.StatBonuses` entries remain percentage-based when resolved with the owning `Character`, but positive percentage suffixes now contribute at least **+1** when the computed integer bonus would otherwise floor to 0 (e.g. a low-level `STR:2` suffix no longer disappears). **Literal:** ACCURACY, HIT, COMBO, CRIT, CRIT MISS, RollBonus. Tests: `EquipmentSystemTests` low-level STR percent minimum, high-STR percent scaling, HIT literal, RollBonus literal; `OVERVIEW.md`.

- [x] **UI / Inventory ? red category bracket when item is unequippable:** Bag rows now draw their category bracket (`[Head]` / `[Body]` / `[Legs]` / `[Feet]` / `[Mace]` / `[Sword]` / `[Dagger]` / `[Wand]`) in **red** when the item's `attributeRequirements` exceed the character's effective STR/AGI/TEC/INT (`Item.GetEquipBlockedReason`), so the player can spot unequippable loot at a glance instead of only finding out at confirmation. Added `ItemRendererHelper.IsEquipBlockedForCharacter`; `RenderItemName` now takes an optional `Character?` and applies `Colors.Red` to just the slot bracket (rarity bracket and item name keep their existing colors). Wired through inventory list (`InventoryScreenRenderer`), the equip-from-bag selection prompt (`ItemSelectionRenderer.RenderItemSelectionPrompt`), the trade-up resulting-item preview (`ItemSelectionRenderer.RenderTradeUpPreview`), and the item comparison view (`ItemComparisonRenderer`). Tests: `ItemRendererHelperBlockedSlotTests` (null character / no requirements / met / unmet / post-normalization technique key); `OVERVIEW.md`.

- [x] **Bugfix / Equipment ? armor unequippable due to `techinque` typo + dict-key normalization:** `GameData/Armor.json` had 48 entries whose `attributeRequirements` dictionary used `"techinque"` instead of `"technique"`. `Item.GetEffectiveValueForRequirementKey` only matches the canonical lowercase keys (`strength`/`agility`/`technique`/`intelligence`) and returns **0** otherwise, so every affected armor piece reported the player had **0** of that stat and refused to equip ? regardless of actual technique. Fixed the data, and made `GameDataJsonNormalizer.MergeSheetAbbrevAttributeRequirementsIfPresent` canonicalize **dictionary** `attributeRequirements` keys too (abbrev `STR`/`AGI`/`TEC`/`INT` and known typos like `techinque` ? `technique`, `inteligence` ? `intelligence`, etc.). Test: `ItemGeneratorTests.TestArmorData_NormalizerCanonicalizesAttributeRequirementKeys`. `OVERVIEW.md`.

- [x] **Bugfix / UI ? title screen DEMON bottom piping + centering:** DEMON?s bottom 3D footing row now matches the customized top (3-pillar **M**, wide-gap **N**, length 46). Removed hard-coded left shifts (`globalLeftShift` / `titleOffset` / tagline indent / press-key `-6`) so title words, decorator, tagline, and ?Press any key?? are true center-justify. Tests: `TitleScreenAsciiSpacingTests`; `OVERVIEW.md`.

- [x] **Bugfix / UI ? title screen ASCII spacing:** Title word glyphs (e.g. top line of **DEMON**) render with incorrect spacing when colored segments are stitched together. Fix title frame building so segment concatenation preserves the original ASCII art without injecting extra spaces; add a regression unit test under UI test suite.

- [x] **Data / Sheets ? ENEMIES layout A?U + spawn system:** Updated `JsonArraySheetConverter` for Region-first two-row headers, separate `HEALTH` band, and mixed-case normalization. Added `region`/`biome`/`location`/`rarity` on `EnemyData`; `EnemySpawnFilter` filters by placement and rarity-weighted picks; `EnemyRarityRewardHelper` scales gold/XP; `Dungeon` threads `SpawnRegionId`. Tests: `JsonArraySheetConverterTests.EnemiesNewSheetLayoutImport`, `EnemySpawnFilterTests`, `EnemyRarityRewardTests`; `OVERVIEW.md`.

- [x] **Bugfix / Enemies ? missing damaging actions warnings:** `GameData/Enemies.json` had `actions` / `isLiving` / `description` / `tags` (and misplaced `baseHealth`) nested under `growthPerLevel`, so `EnemyData.Actions` deserialized empty. Normalized all rows to canonical root fields; fixed **Magma Beast** `actions` (was a broken JSON string). `EnemyLoaderTests` regression already covers damaging actions on Goblin.

- [ ] **Bugfix / UI ? Action Lab button hover disappears:** Hovering the ?Open Action Lab? button causes it to vanish (pointer-over visual state renders transparent / hidden). Identify the style/template and fix hover states so the button stays visible; add a regression UI/unit test that asserts the computed pointer-over style keeps `Opacity` \(>\) 0 (and foreground contrast remains readable).

- [ ] **Bugfix / Enemies ? base attributes all 3:** Enemy STR/AGI/TEC/INT are coming out as `3` for every enemy in runtime. Trace load/build pipeline (`EnemyLoader` ? `EnemyFactory`/`EnemyBuilder` ? `EnemyStatCalculator`) and ensure per-enemy `baseAttributes` from `GameData/Enemies.json` are applied (and only fallback logic runs when fields are omitted). Add a unit test that loads at least 2 enemy templates with different base attributes and asserts they differ at runtime.

- [ ] **DevEx / Scripts:** Consolidate `Scripts/` into a small set of core commands (build/run/test/clean/metrics) and move legacy Google Sheets + redundant wrappers into `Scripts/_legacy/`. Update script docs and keep a single entrypoint.
- [ ] **Combat / INT:** Replace INT ?+roll bonus per N? with milestone-based threshold bonuses that grant +1 to HIT/COMBO/CRIT at specific effective-INT breakpoints (as per the tuning table). Ensure combat resolution, action selection, and HUD threshold preview all agree; add unit tests.
- [ ] **Loot / Rarity:** Remove **Transcendent** as a rarity category; **Mythic** is the highest. Update loot rarity rolls, item-gen lab fixed chances, color themes, and unit tests accordingly.
- [x] **UI / Item Generation:** Top **Auto** scroll area ? **Generation Parameters** (2?4 grid + rarity preview), **lab rarity** + **live tier** strip, **Clear**/**Generate**, and **Affix bounds** in a collapsed **Expander** (inner affix table + notes in a `ScrollViewer` capped at **400** px height). Bottom **`*`** row is only **Generated list** | **Selected** (`ItemGenerationSettingsPanel.axaml`).
- [x] **UI / Item Generation:** Item Generation tab uses a **2-column** list row (**Generated list** | **Selected Item**); **Generate** and **Clear** live in the **top** control strip (supersedes earlier ?Generate in Selected only? layout).
- [ ] **UI / Item Generation:** Add a **Statistics** section near ?Selected Item? that shows counts for each **tier ? rarity** combination in the current generated batch, plus how many generated items are **duplicates** (repeats of an identical item in the batch).
- [ ] **UI / Item Generation:** Fixed rarity chances ? add **Base ratio** input so each rarity is X? rarer than the previous, and auto-derive normalized percentages (Common?Mythic) for the lab.
- [x] **UI / Item Generation:** Fixed rarity chance boxes ? improve hover readability (remove hard-coded TextBox colors so hover style applies; ensure text stays high-contrast).
- [x] **UI / Item Generation:** Remove the top ?Item Generation Lab? banner section in Settings ? Item Generation (title + subtitle card).
- [x] **UI / Item Generation:** Change **Dungeon level** input to **Dungeon ?** (relative to hero). Enter `+5` so hero 10 implies dungeon 15; clamp final dungeon level 1?99; update preview copy + tests.
- [x] **UI / Item Generation:** Move **Hero / dungeon** inputs into the top parameter rows so the rarity + tier preview tables can use the full panel width.
- [x] **UI / Item Generation:** Rename affix-bounds group labels to **Prefix / Suffix / Actions** (keep helper copy clarifying **Suffix** = `StatBonuses.json` suffix lines, distinct from prefix-slot modifications that can also change stats).
- [x] **UI / Item Generation:** Clarify affix table column headers (prefix slots vs stat-bonus suffix lines vs action bonus lines) so abbreviations aren?t misread as ?suffix? in general.
- [x] **UI / Item Generation:** Tighten copy so ?Stat bonus suffixes? reads as **StatBonuses lines** (distinct from modification/prefix-driven stat effects).
- [x] **UI / Item Generation:** Widen the ?Affix bounds by rarity? grid so column labels and values don?t clip/abbreviate in the Settings ? Item Generation tab.
- [x] **UI / Item Generation:** Generation Parameters panel ? cleaner layout (three equal columns, fixed-width count/hero level, rarity preview callout, actions right-aligned).
- [x] **UI / Item Generation:** Tier roll preview under rarity summary (loot level from hero vs dungeon 1, `LootTierCalculator.GetTierRollPreview`; tests in `LootTierCalculatorTests`).
- [x] **Item Generation Lab:** When **Tier** is **Any**, batch generation rolls tier with `LootTierCalculator` (hero + `DungeonLevel` ? loot level ? `TierDistribution.json`), same as `LootGenerator` ? not uniform 1?5. Tests: `ItemGenerationLabServiceTests.TestAnyTierUsesTierDistributionAtLootLevel1` / `AtLootLevel19`.
- [x] **UI / Item Generation:** Dungeon ? input added next to hero level; tier preview and generation use hero-vs-(hero+?) for loot level and tier rolls.
- [x] **UI / Item Generation:** Fixed rarity chance inputs (Common?Transcendent) to control lab rarity rolls when Rarity is Any (ignores level gating). Tests: `ItemGenerationLabServiceTests.TestFixedRarityChancesOverridesAnyAndIgnoresLevelGates`.

## Completed (reference)

- [x] **Data / Loot ? slot affix tuning + combo cap + armor columns:** Per?item-type affix overrides (`ItemAffixByRaritySettings.PerItemType`) with legacy `PerRarity` fallback; `LootBonusApplier` passes `item.Type`; combo sequence cap from `LootSystemConfig` + feet `ExtraActionSlots` + suffix `ExtraActionSlots` (`ComboSequenceMaxHelper`, `ComboSequenceManager`, inventory handlers, trim on gear change); armor catalog fields (`ArmorData` / `Item` base stats, `minActionBonuses` ? `MinGeneratedActionBonuses` floor for granted action lines); sheet headers + `JsonArraySheetConverter` enemy import fix (category carry must not swallow `actions` / trailing columns); `ItemGenerationSettingsPanel` slot selector + combo cap inputs; tests (`ItemConfigTests`, `LootBonusApplierTests`, `ComboSequenceMaxHelperTests`, `JsonArraySheetConverterTests`, combo suites with temporary cap raise). `OVERVIEW.md`.

- [x] **Loot / affix-line rarity:** `StatBonuses.json` rows use **`Rarity`** (affix pool tier) instead of legacy **`Weight`**. Each rolled stat suffix and each prefix slot rolls a tier weighted from **`RarityTable.json`**, then picks uniformly among rows in that tier (`StatBonus.Rarity`, `Modification.ItemRank`; blank rank ? Common). Sheets CSV import maps legacy **Weight** only to **Rarity Common** when **Rarity** is absent. **`JsonArraySheetConverter`**, **`LootBonusApplier`**, **`LootDataCache`**, lab **`CloneStatBonus`**. Tests: **`JsonArraySheetConverterTests`**, **`LootBonusApplierTests`**. **`OVERVIEW.md`**.

- [x] **UI / Dungeon selection:** Custom difficulty ? typed dungeon level shows inline on the **Custom difficulty** row (`Level:` + digits + `_` cursor); removed full-screen `ShowMessage` for this flow; validation uses bottom non-blocking messages (`DungeonSelectionHandler`, `DungeonSelectionRenderer`, `CanvasUICoordinator`).
- [x] **Data / New game:** Only **four** starting weapons on the pick-one menu ? removed duplicate **starter** tag from the second identical Wand **STICK** row in `Weapons.json`; `OVERVIEW.md` documents starter-tag vs tier-1 fallback.
- [x] **Loot / rarity weights:** Set `RarityTable.json` weights to pure powers of 10 (Common?Transcendent) for clean ?1 in N? mental math; document resulting odds.
- [x] **Bugfix / items:** First stat suffix duplicated in display names when two `StatBonuses` names had the same length (`GetBaseItemName` only stripped the outer suffix in one pass). Fixed with iterative stripping; test `ItemGeneratorTests` equal-length suffix idempotency case.
- [x] **UI / Item Generation Lab:** Rarity **Any** rolls per item with `LootRarityProcessor` (fixes 100% Common bug). **Hero level** UI + `ItemGenerationSpec.PlayerLevel` for those rolls and Epic+ gates; `ItemGenerationLabServiceTests.TestAnyRarityRollsNonCommonDistribution`.
- [x] **Loot / probabilistic affix tuning:** Per-rarity **minimum** prefix slots, stat suffixes, and action bonuses in `TuningConfig.json` (`itemAffixByRarity`) with optional **extra chance** (0?100%) and **max** caps; `LootBonusApplier` rolls final counts each drop. Settings **Item Generation** tab grid for load/save (`ItemGenerationPanelHandler`, `ItemAffixRollRule`, `RollAxis`). Tests: `LootBonusApplierTests.TestProbabilisticAffixTuningHitsMax`, `ItemConfigTests` (extra JSON + `RollAxis`). `OVERVIEW.md`.

- [x] **Item Generation Lab / names:** Triple prefix in the lab (and any double call to `GenerateItemNameWithBonuses`) fixed: `ItemGenerator.GetBaseItemName` strips leading prefix-slot words and trailing suffix-style tokens so name assembly is idempotent; `ItemGenerationLabService` no longer re-runs name generation after `LootBonusApplier.ApplyBonuses`. Test: `ItemGeneratorTests` idempotency case.
- [x] **Settings / Item Generation Lab:** New Settings tab ?Item Generation? with forced generation controls (rarity/tier/weapon type/armor slot/count) and a generated list sorted best?worst (rarity ? tier ? primary stats). Code: `ItemGenerationSettingsPanel`, `ItemGenerationLabService`. Tests: `ItemGenerationLabServiceTests`. `OVERVIEW.md` updated.
- [x] **Loot / item prefixes:** Three-slot prefix system (Adjective / Material / Quality) with rarity rules (Common 1 random slot, Uncommon 2 random, Rare+ all three), ordered naming, `gearPrimaryStatMultiplier` for quality, material attribute effects on equipment stats, merged `PrefixMaterialQuality.json`, Divine reroll resolves to a replacement adjective, `RarityTable.json` prefix counts cleared. Code: `ItemPrefixHelper`, `LootBonusApplier`, `Item.cs`, `EquipmentBonusCalculator`, `LootDataCache`, `ItemGenerator`, `ItemNameParser`, lab clone `PrefixCategory`. Tests: `ItemPrefixHelperTests`, `ContextualLootTest` adjective-only thematic count; `OVERVIEW.md`.

- [x] **UI / Settings ? tags visibility:** Actions tab ? selected-action runtime tags preview (`ActionsSettingsPanel`, `ActionsTabManager`). Items ? `TagsSummary` on weapon/armor rows, **Tags** field in `ItemEditDialog`, load/save/rename via `ItemsDataCoordinator` / `ItemsTabManager` + `GameDataTagHelper.ParseCommaSeparatedTags`. Enemies ? new **Enemies** category (`EnemiesSettingsPanel`, `EnemiesTabManager`, `EnemiesDataService`), save path in `SettingsSaveOrchestrator` + `EnemyLoader.LoadEnemies()` after write. Tests: `GameDataTagHelperTests`; `OVERVIEW.md`.

- [x] **Data / Sheets ? selective push:** Per-tab OAuth push toggles in `SheetsPushConfig.json` (`pushActionsTab`, `pushWeaponsTab`, ?); `GameDataSheetsPushService` / preflight respect flags; Balance Tuning panel checkboxes load/save with `SheetsPushConfig.Load` migration for legacy JSON (missing keys ? push enabled); tests `SheetsPushConfigTests` (`ApplyMissingPushTabDefaults`); `SheetsPushConfig.template.json`; `OVERVIEW.md`.

- [x] **Data / tags:** ACTIONS sheet **`TAGS`** row-2 column ingests into `SpreadsheetActionData.Tags`; optional **`tags`** on `Weapons.json` / `Armor.json` / `Enemies.json` (sheet columns + JSON arrays); `ItemGenerator` copies catalog tags to `Item.Tags`; `GameDataTagHelper`; validators warn on empty tag entries; tests `SpreadsheetActionDataSheetRowSerializerTests.TestTagsColumn_IngestsAndConverts`, `JsonArraySheetConverterTests` (weapons/armor/enemies tags round-trips), `ItemGeneratorTests` (copy tags); `OVERVIEW.md`.

- [x] **Saves / death:** On player death, persist tombstone (`isDead`, `character_*_dead.json`), remove live save, exclude from load list and refuse explicit load; clear-all removes dead files; tests `SaveLoadSystemTests.TestDeadCharacterTombstone`; `OVERVIEW.md` (Saves ? death tombstones).

- [x] **UI / combat log:** Damage lines render the **hits** keyword in **white** (including multi-hit `(N hits)` wording); `CombatColorStrategy.GetHitsColor`, `DamageFormatter.FormatDamageDisplayColored`; `OVERVIEW.md`.

- [x] **UI / HUD:** Left HERO panel ? hero **name** uses per-glyph class color cycles once the HUD title leaves the default **Fighter** (no-points / pre?tier-1 gate) state; palettes for each solo weapon path and each hybrid pair (top two paths by points). `HeroNamePanelColoredText`, `CharacterPanelRenderer`; tests `HeroNamePanelColoredTextTests`; `OVERVIEW.md` (HUD ? left panel hero name).

- [x] **Bugfix / combat:** Below INT 10, combo-path strip index no longer rolled a random slot each swing (strip showed slot 0 e.g. **CAST** while combat could resolve **STAB**); `ActionUtilities.ResolveComboStripIndex` now uses `ComboStep % count` when salt is null, matching HUD and encounter reset. `ActionSequenceTests.TestComboStripIndexRespectsIntelligenceThreshold`; `OVERVIEW.md` combo bullet.

- [x] **Bugfix / combat:** Combo AMP tiers follow **strip slot index** (first action 1.00?, second = TECH baseline, then scaling); opener/finisher flags no longer invert tiers vs sequence. `ActionUtilities.GetComboAmplificationExponent`, `TryGetComboActionSlotIndex`; tests `ActionUtilitiesTests`, `ComboExecutionTests`; `OVERVIEW.md`, tooltip copy.

- [x] **Combat / state:** Hero status effects and temp combat state wipe after each combat, after each survived room, and on dungeon completion or early exit (`CombatManager`, `RoomProcessor`, `DungeonOrchestrator`); `Actor.ClearAllTempEffects` extended for advanced stack fields; `CharacterFacade.ClearAllTempEffects` ? `Character.ClearAllTempEffects`; tests `ActorClearTempEffectsTests`; `OVERVIEW.md`.

- [x] **Bugfix / UI:** Inventory ? hover tooltips for bag rows (and left character panel while inventory is open): `InventoryScreenRenderer` no longer clears shared clickables after the character panel registered `lphover` targets; inventory `lphover` uses a full chrome refresh so tooltip paint does not erase the item list; `LeftPanelTooltipBuilder.AppendGear` adds an **Actions:** line from `GearActionNames.Resolve` via `GetGearActions`; tests `LeftPanelTooltipBuilderTests`; `OVERVIEW.md`.

- [x] **UI / Inventory:** Right panel **INVENTORY** list under **POOL** ? actions from bag items (`InventoryActionPoolEntries`); row click equips (slot swap, old piece to bag) via `cpi:invpool:N` + `InventoryComboManager` / `InventoryItemActionHandler`; adds that action to the sequence when a matching combo pool instance exists. Combo management center panel mirrors the block. Tests: `ComboPointerInputTests`, `InventoryActionPoolEntriesTests`; layout reserve synced in `InventoryRightPanelLayout`; `OVERVIEW.md`.

- [x] **Data / New game:** Starter weapons unified with `Weapons.json` ? menu + equipped item use first tier-1 row per class path; `StartingGear.json` optional armor (default none); `StartingGearLoader`, `GameInitializer`, weapon UI/lab dialog, tests, `OVERVIEW.md`.
- [x] **Data / New game ? default slots:** Head / body / feet start unequipped unless data specifies armor (`StartingGear.json`, tuning `startingGear.armor`, or **starter** tags in `Armor.json`); inventory empty after init (`GameInitializer`, `GameInitializerTests`).

- [x] **UI:** Action Lab gear editor ? **Tier** filter on weapon, head, body, and feet dialogs (`ActionLabWeaponEditDialog`, `ActionLabGearCatalogFilter.ItemMatchesTierFilter`, tests in `ActionInteractionLabTests.ActionLabGearCatalogFilter_Basics`); `OVERVIEW.md`

- [x] **Bugfix / combat:** Combo-strip vs normal selection used preview attack total and ACC-shifted threshold, so a low d20 (e.g. 9) could still execute a **14+** combo special. Selection now compares the modified die only to the threshold with COMBO adjustments but **without** ACC lowering the gate; INT/stat roll bonuses no longer bypass the die. `ActionSelector`, `ActionSelectorRollBasedTests`, `OVERVIEW.md` (combo bullet), `LeftPanelTooltipBuilder` (combo threshold tip).

- [x] **Bugfix / combat:** Weapon required basic was not in the pool when a weapon only specified other actions (`GearAction` / bonuses), so `WeaponRequiredComboAction` could not protect or re-inject it. `GearActionNames.Resolve` now prepends the resolved required basic when missing; test `GearActionNamesTests.TestWeaponWithGearActionOnly_IncludesRequiredBasicName`; `OVERVIEW.md` (combo bullet).

- [x] **UI:** Action Lab armor gear (head / body / feet) ? **Armor class** + **Rarity** filters aligned with weapon dialog behavior (`ActionLabWeaponEditDialog`, `ActionLabGearCatalogFilter`, tests `ActionInteractionLabTests.ActionLabGearCatalogFilter_Basics`); `OVERVIEW.md`

- [x] **Combat / DoT:** Poison, burn, and bleed from weapon mods (`weaponPoison` / `weaponBurn` / `weaponBleed`), weapon `StatusEffects`, and unconditional action flags apply on **critical hit** only (`CombatEffectsSimplified`); UI strings; tests `CombatEffectsSimplifiedTests`, `CombatResultsTests`; `OVERVIEW.md`

- [x] **Progression / XP:** Dungeon-paced level bars ? one tier-1 dungeon completion to L2, then ~1.5, 2, 3, 4? completions worth per level (`CharacterProgression.GetXpRequiredToAdvanceFromLevel`, `GetBaseXpForContentLevel`, `GetStandardDungeonCompletionXpForLevel`, `GetExpectedDungeonsToLevelFromLevel`); `XPRewardSystem` / `RewardManager` share completion math; tuning docs (`ProgressionConfig`, variable editor), `OVERVIEW.md`, `XPSystemTests.TestDungeonPacedXpRequirementCurve`.

- [x] **Bugfix / UI:** Combat log ? no blank line between a **DoT tick on the hero** (burn/poison/bleed block) and that hero?s **next** combat action; `TextSpacingSystem` tracks `lastDoTAfflictedEntity` on `RecordBlockDisplayed(PoisonDamage, ?)`, `StatusEffectProcessor` passes `entity.Name`; test `TextSpacingSystemTests.TestDoTSpacingRules`; `OVERVIEW.md` (DoT ? combat log spacing).

- [x] **Progression / bugfix:** Mid-dungeon `AddXP` double-counted `Level` (progression + `LevelUpManager.LevelUp`), so one threshold could show ?level 3? with only one class point. Fixed `CharacterFacade.AddXP`; multi-step `AddXPWithLevelUpInfo` now passes per-step display level. **XP curve** wired to `BaseXPToLevel2` + `XPScalingFactor` (flat default). Tests: `XPSystemTests`, `MultiSourceXPRewardTests`, `LevelUpSystemTests`, `GameplayFlowTests`; `OVERVIEW.md` (Progression ? XP subsection).

- [x] **Bugfix / UI:** Right panel LOCATION ? dungeon/room/enemy names use full inner panel width for ellipsis (fixes **Tomb of the forgotten** truncated at 20 chars). `RightPanelContentText`, `RightPanelRenderer`, tests `RightPanelContentTextTests`; `OVERVIEW.md`

- [x] **Bugfix / combat:** Enemies below combo threshold no longer fall back to the first combo-flagged special when the pool has no non-combo basics; they use the same unnamed synthetic normal attack as heroes. Enemy combo-threshold preview uses the resolved strip slot (matches hero logic). `ActionSelector`, tests `ActionSelectorRollBasedTests.TestEnemyComboOnlyPoolBelowThresholdIsUnnamedNormal`, `OVERVIEW.md`

- [x] **Combat / combo:** Reset full hero combo state (`ResetCombo`) at new encounter init, new dungeon room, and dungeon start; enemy reset on encounter init; `RunCombat` uses `ResetCombo` instead of only `ComboStep = 0`; test `CombatStateManagerTests.TestInitializeCombatEntitiesResetsPlayerAndEnemyCombo`; `OVERVIEW.md`

- [x] **UI / Feature:** Inventory ? **right-click** a filled **action strip** card removes that combo sequence slot (same as `cpi:rm`; blocked during equip/compare/trade prompts; weapon-required basics protected). `InventoryMenuHandler.TryHandleStripRightClickRemove`, `GameCoordinator.TryHandleInventoryStripRightClickRemove`, `MouseInteractionHandler.TryHandleActionStripRightClickRemove`; tests `InventoryMenuStripRightClickTests`; `OVERVIEW.md`

- [x] **Bugfix / combat:** Low-INT chaotic combo-strip pick called `Dice.Roll(1, stripCount)`; with **one** combo action that became invalid `1d1` and aborted the dungeon at combat start. Fixed in `ActionUtilities.ResolveComboStripIndex`; regression `ActionSequenceTests.TestComboStripIndexSingleSlotLowIntDoesNotThrow`; `OVERVIEW.md` (combo bullet).

- [x] **Data:** `GameData/Enemies.json` ? every enemy now has full **`baseAttributes`** (STR/AGI/TEC/INT) and explicit four-stat **`growthPerLevel`**; legacy root stat keys removed (weights were merged into `growthPerLevel` so normalized 6pt/level behavior matches prior fold); `OVERVIEW.md` already describes base + growth.

- [x] **Data / Sheets:** Remove enemy `overrides` from the model and ENEMIES sheet; **STR+AGI+TEC+INT** `growthPerLevel` normalized to **6 points/level** (`EnemyStatCalculator`, `EnemyDataPostLoad` for legacy `overrides.health`); legacy root stats fold into **growth only**; canonical columns **A?P**; tests `EnemyAttributeGrowthTests`, `JsonArraySheetConverterTests`, `TuningSystemTest`; `OVERVIEW.md`

- [x] **Data / Sheets:** ENEMIES legacy root stats (`strength`, `agility`, ?) fold into `growthPerLevel` on load (historically also `overrides`); CSV import hoists them; push maps roots into canonical columns (`EnemyData.ExtensionData`, `EnemyDataLegacyRootStats`, `JsonArraySheetConverter`); tests `JsonArraySheetConverterTests`, `EnemyAttributeGrowthTests`; `OVERVIEW.md`

- [x] **Bugfix / data:** Partial `baseAttributes` / `growthPerLevel` in `Enemies.json` no longer zeroed unspecified stats (nullable `EnemyAttributeSet` + `EnemyStatCalculator` docs); regression `EnemyAttributeGrowthTests.TestPartialBaseAttributesFallsBackForOmittedStats`; `OVERVIEW.md`

- [x] **UI:** Action Lab ? right-panel enemy **level** line shows **hero delta** (`Lvl 9 (-2)` when hero is 11); `ActionLabRightPanelEnemyAdjustment.FormatEnemyLevelCaptionWithHeroDelta`, `RightPanelRenderer`; test `ActionInteractionLabTests.EnemyLevelCaption_ShowsHeroDelta`; `OVERVIEW.md`

- [x] **Bugfix / UI:** Action Lab **[ Back ]** no longer clears the combat log and visibly re-runs remaining steps; undo mutes entity replay and restores a per-step center-log snapshot (`ReplayHistoryAsync`, `LabCombatLogSnapshot`, capture/restore in `Game.EnterActionInteractionLab`). Input single-flight + `InputEpoch` drops queued Steps after Back/Reset. Tests: `ActionInteractionLabTests`; `OVERVIEW.md`

- [x] **UI / Feature:** Action Lab tools ? **[ Reset ]** (was ?Reset combo?): clears combat log and step/sim counters, refills HP, clears status/temp effects, zeros combo steps; keeps gear, combo strip, and current enemy (`ActionInteractionLabSession.ResetLabEncounterAsync`, `ActionLabControlsRenderer`, `ActionLabInputCoordinator`); tests `ActionInteractionLabTests`; `OVERVIEW.md`

- [x] **Combat / combo:** INT-gated combo strip ? effective INT &lt; 10 randomizes which combo action is used on combo-path selection; INT ? `GameConstants.ComboSequenceIntelligenceThreshold` (10) keeps `ComboStep` order (`ActionUtilities.ResolveComboStripIndex`, `ActionSelector`, hero ACTION cadence slot from selected action in `ActionExecutionFlow`). **Low INT also skips `ComboRouter` on `Character.IncrementComboStep`** (linear next slot only). Tests: `ActionSequenceTests` (INT threshold + pending slot + `TestLowIntIgnoresComboRoutingOnComboStepAdvance`), `ActionInteractionLabTests.EnemyComboSelectionUsesComboStepIndex`; `OVERVIEW.md`

- [x] **Bugfix / UI:** Hero threshold panel ? queued ACC/COMBO/HIT preview no longer adds stat roll bonus to threshold shifts (matches combat: roll bonus on attack total, FIFO ACC on thresholds); `ClampDiceLadderDisplayValue` floors display; `ThresholdDisplayFormattingTests`; `OVERVIEW.md`

- [x] **UI / Feature:** Action Lab ? **hero level** (left panel) applies the same level delta to the **lab enemy** when the hero?s level changes; **enemy level** (right panel) stays independent (`ActionLabLeftPanelStatAdjustment`, `ApplyLabEnemyLevelDelta`); test `ActionInteractionLabTests.LeftPanelHeroLevelSyncsLabEnemy_EnemyRowIndependent`; `OVERVIEW.md`

- [x] **Combat / combo:** Per **weapon type**, one **required basic** action must stay in the player?s combo sequence (`weapon_basic` + `weaponTypes` in `Actions.json`; `WeaponRequiredComboAction`, combo restore/gear rebuild ensure; removal blocked unless a duplicate copy remains). Tests: `WeaponRequiredComboActionTests`; `EntitySystemTestRunner`; `OVERVIEW.md`

- [x] **UI / Feature:** Action Lab tools ? **Actions taken** counter (`LabTotalActionTicks`, batch-sim turn sum, reset on fight history clear); `ActionLabControlsRenderer`, `ActionLabInputCoordinator`, `ActionInteractionLabSession`; tests `ActionInteractionLabTests.LabTotalActionTicks_StepUndoSimAndFightReset`; `OVERVIEW.md`

- [x] **Bugfix:** Weapon poison (and other on-hit status effects) applied **twice** when using the colored combat path ? `ActionExecutionFlow` already called `ApplyStatusEffects`; `ExecuteActionWithStatusEffectsColored` called it again. UI now parses `StatusEffectMessages` only; regression test in `CombatResultsTests`; `OVERVIEW.md` DoT note

- [x] **UI:** Action Lab weapon dialog ? **Weapon type** + **Rarity** filters (prefix/suffix/weapon lists); optional `StatBonus.ItemRank`; `ActionLabGearCatalogFilter` + tests; `OVERVIEW.md`

- [x] **UI:** Action Lab open ? **multi-window placement**: center main window on its monitor, right-anchor lab tools in working area, left-nudge pop-out settings on same monitor; tests `ActionLabWindowPlacementTests`; `OVERVIEW.md`

- [x] **UI:** Action Lab gear editor dialog ? default ~1000px height, user-resizable, grid layout so weapon/prefix/suffix lists share vertical space (`ActionLabWeaponEditDialog`); `OVERVIEW.md`

- [x] **Feature:** Action Lab gear editor ? **multi-select** prefixes (modifications) and suffixes (stat bonuses) when right-clicking gear; `ActionLabWeaponEditDialog`, `ActionLabWeaponFactory` / `ActionLabArmorFactory` list overloads; tests + `OVERVIEW.md`

- [x] **UI:** Right combat panel ? under Status Effects, show **Immune: Bleed, Poison** when enemy template has `isLiving` false (`Enemy.IsLiving`); tests + `OVERVIEW.md`

- [x] **Combat / data:** `weaponPoison` roll ranges ? **Venomous** `MinValue`/`MaxValue` **1?2** and **Venomous Blade** **2?3** in `Modifications.json` (was 1/1 for both, so Blade only added **+1%** per hit instead of **+2?3%**); `OVERVIEW.md` DoT note

- [x] **Data / Sheets:** Item suffixes tab ? `statBonusesSheetUrl` / `statBonusesSheetTabName`, `GameDataTabularSheetKind.StatBonuses`, pull/push `StatBonuses.json`; Balance Tuning gid field; `SheetsConfig.json` gid `388294050`; tests + `OVERVIEW.md`

- [x] **Combat:** Poison / burn / bleed revamp ? poison as % of max HP (monotone on apply); burn intensity + pending on 5s game tick; bleed same intensity math on actor turn end; deterministic weapon mods; handlers, UI lines, `Modifications.json`, tests + `OVERVIEW.md` DoT subsection

- [x] **Data / Sheets:** Dungeons tab ? `dungeonsSheetUrl` / `dungeonsSheetTabName`, `GameDataTabularSheetKind.Dungeons`, pull/push `Dungeons.json`, pipe-separated `possibleEnemies` on import; tests + `OVERVIEW.md`

- [x] **Data / Sheets:** Enemies & environments spreadsheet import support
  - [x] `JsonArraySheetConverter`: full `EnemyData`-aligned canonical headers; `enemies` on environments; pipe-separated `actions` normalization for ENEMIES CSV
  - [x] `RoomData` + `RoomEnemyData`, `RoomLoader` ? `Environment` ? `EnemyGenerationManager` weighted room spawn pool; `RoomDataValidator` + `ReferenceValidator` for `enemies` references
  - [x] Tests: `JsonArraySheetConverterTests` (full enemy stats round-trip, pipe actions, environments with `enemies`)
  - [x] Docs: `OVERVIEW.md` (Sheets subsection) + this task list

- [x] **UI:** Action Lab controls pop-out ? show more enemy type rows, fewer catalog rows (`ActionLabControlsRenderer`).

- [x] **Feature:** Action Lab ? interactive enemy level on right combat panel
  - [x] Session: `_labEnemyBaseLevel`, `_labPanelEnemyLevelDelta`, `ApplyLabEnemyLevelDelta`, `BuildLabEnemyFromPanelState`, restore + `CaptureSimulationSnapshot`
  - [x] UI: `Lvl` row + `rphover:enemy:level` click target when rendering lab combat; DMG/Spd lines aligned with direct-stat enemies
  - [x] Input: `ActionLabRightPanelEnemyAdjustment` + `MouseInteractionHandler` left/right click
  - [x] `TestCharacterFactory.CreateTestEnemy(..., level)` scaling; `Enemy.UsesDirectCombatStats` / `GetEffectiveStrength` for direct damage
  - [x] Tests: `ActionInteractionLabTests`, `ActionLabEncounterSimulatorTests`
  - [x] Docs: `OVERVIEW.md` + this task list

- [x] **Data:** Enemy + environment actions in `GameData/Actions.json` (spreadsheet format) with `enemy` / `environment` tags; loader and weapon fallbacks exclude them from the hero.

- [x] **Bugfix:** Hero base stats ?MultiHit mod? applies to the next action (see `OVERVIEW.md` milestone note)

