# count-lines

Runs the PowerShell script to count production code lines in .cs files (excluding test classes) and identify files above 400 lines threshold.

Compares production code vs test utilities to show clean metrics.

@count-cs-lines-no-tests.ps1

This command will be available in chat with /count-lines
