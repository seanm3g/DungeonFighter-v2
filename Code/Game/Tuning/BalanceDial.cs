namespace RPGGame.Tuning
{
    /// <summary>
    /// PDF balance dial taxonomy: average wrong → Power, too random → Variance,
    /// player helpless → Agency, progression wrong → Scaling.
    /// </summary>
    public enum BalanceDial
    {
        Power,
        Variance,
        Agency,
        Scaling
    }
}
